# Next Session Context

Last updated: 2026-06-11

## What Was Audited

Completed Stage 0 through Stage 9.

- Stage 0 created the audit workspace and all required audit files.
- Stage 1 mapped PIVX/Sapling integration paths.
- Stage 2 audited Sapling shielded receiving only.
- Stage 3 audited Sapling shielded sending and related transaction construction/broadcast state only.
- Stage 4 audited PIVX balance and wallet state correctness.
- Stage 5 audited PIVX seed, keys, derivation, restore, recovery, secure storage, and key leakage only.
- Stage 6 audited PIVX network and configuration only.
- Stage 7 audited PIVX mobile security only.
- Stage 8 audited PIVX UI/UX only.
- Stage 9 produced the release gate checklist and manual test plan only.
- No production code was modified.

Stage 9 focused on:

- External APK/TestFlight release blockers from Stages 2-8
- Must-fix gates before external APK and TestFlight
- Node/Sapling RPC and mainnet/testnet release policy requirements
- Restore, receive, send-route, sync/error, backup/security, QR/URI, and UI manual tests
- Release tester guidance and minimum pass set for limited external readiness

## Current Verdicts

Stage 2 verdict: NOT READY for external APK or TestFlight shielded receiving.

Stage 3 verdict: NOT READY for external APK or TestFlight shielded sending.

Stage 4 verdict: NOT READY for external APK or TestFlight PIVX balance state.

Stage 5 verdict: NOT READY for external APK or TestFlight PIVX restore/key-derivation coverage.

Stage 6 verdict: NOT READY for external APK or TestFlight PIVX network/configuration.

Stage 7 verdict: NOT READY for external APK or TestFlight PIVX mobile security.

Stage 8 verdict: NOT READY for external APK or TestFlight PIVX UI/UX.

Stage 9 verdict: NOT READY for external APK or TestFlight PIVX release.

## Stage 9 Artifacts

Stage 9 created:

- `09_release_gate_checklist.md`: current release verdict, APK/TestFlight blockers, must-fix gates, broader beta gates, manual confirmation requirements, release tester guidance gate, and exit criteria.
- `10_test_plan.md`: manual test plan covering mobile platforms, node/Sapling RPC setup, network policy, wallet creation, restore, receive, send routes, balances/history, sync/error states, backup/security, QR/URI, UI/UX regressions, and release tester dry run.

Most important Stage 9 release blockers:

- Correct Sapling global tree/output position tracking and restart/resume nullifier correctness.
- Stop treating failed Sapling RPC ranges as empty scanned ranges.
- Encrypt or move PIVX Sapling sidecar state into the wallet's encrypted storage model.
- Sanitize seed restore errors and all shielded metadata logging.
- Define and wire mainnet/testnet policy end to end.
- Freeze and probe the production Sapling ElectrumX RPC contract.
- Implement or explicitly block every transparent/shielded route before transaction creation.
- Fix shielded balance, pending outgoing state, send-max, fee/dust, history, and confirmation semantics.
- Rebuild/prove native Sapling libraries and proving-parameter handling for Android/iOS release.
- Provide external tester guidance before any external build.

## What Remains

Stage 9 is complete. The next work should be implementation or verification against the release gates, not more audit-only staging unless the user asks for another audit pass.

Implementation pass started on 2026-05-27. Local wallet-side changes began closing the highest-risk gates, but external APK/TestFlight prep remains blocked until automated/manual tests pass and server-side ElectrumX work is complete.

Completed or partially completed in the wallet repo:

- Sapling `get_block_range` failures now throw after retries instead of being treated as empty successful ranges.
- Sapling sidecar storage now has an encrypted file path, with legacy plaintext migration when wallet encryption utilities are available.
- Sapling sync now persists a `nextTreePosition` cursor independent of owned notes so restart/resume does not derive the global tree cursor from wallet-owned notes only.
- Seed restore validation no longer throws an exception containing the entered PIVX mnemonic.
- Several shielded note/log lines were redacted to avoid note ids, values, balances, cmu prefixes, nullifier/rseed availability, and addresses in logs.
- PIVX send route creation now blocks mixed transparent/shielded outputs and shielded-to-transparent sends before falling through to transparent transaction creation.
- PIVX send UI state now defaults to transparent source selection instead of Litecoin `nonMweb`.
- PIVX mainnet/testnet wallet creation/opening now passes a PIVX network into the wallet instead of always hard-wiring mainnet.
- Dart/Rust PIVX testnet Sapling activation is now aligned to `201`, based on the ElectrumX agent's PIVX Core v5.6.1 source report. This still requires independent release-owner Core source/commit confirmation and manual testnet validation.
- PIVX z-to-z send-max, fee-aware note selection, and dust/change handling now share a Dart/Rust fee policy. Independent PIVX Core v5.6.1 source evidence confirms min relay fee `10000`, dust relay fee `30000`, wallet min fee `10000`, Sapling fee factor `100`, transparent dust `5,460`, and shielded dust `1,446,000`; Dart/Rust Sapling fee estimation, shielded output checks, and dust-change handling now apply the Core factor-100 and shielded-dust policy. This still requires manual mempool/device acceptance tests.
- PIVX shielded receive and outgoing z-to-z rows now appear in dashboard history with shielded pool/route metadata, pending confirmation labeling, and spend direction.
- PIVX dashboard and Connection/Sync UI now surface shielded sync state, last shielded scanned height, Sapling RPC readiness, and sanitized shielded sync errors separately from generic sync.
- PIVX Sapling capability probing now accepts `blockchain.sapling.capabilities` before `blockchain.sapling.get_capabilities` and preserves advertised contract/server/Core version, network, activation, block-hash, structured-error, and range metadata.
- PIVX node records now persist Sapling capability/version metadata, and the node list displays cached per-node Sapling readiness/version status after probing.
- PIVX v1 node-readiness classification was tightened on 2026-06-02. `SaplingRpcCapabilities.supportsV1ReleaseContract` now requires the reported `pivx.sapling.electrumx.v1` contract id, block-range support, canonical global output positions, best-anchor, anchor-bound witness support, block hashes, structured errors, and the required v1 methods. Incomplete advertised v1 metadata is rejected; legacy block-range fallback is labeled compatibility-only and does not satisfy the release predicate. Manual node testing, node-list status probing, and automatic node switching now use this v1 predicate instead of bare block-range support. This remains open for deployed capable/non-capable v1 node evidence, upgraded default-node metadata, node-list screenshots, and device auto-switch tests.
- PIVX shielded transaction details now show pool, route, and shielded status for shielded receive and z-to-z rows.
- PIVX proving-parameter acquisition now uses Cake's `ProxyWrapper`, verifies cached/downloaded files by expected size and SHA-256, writes to `.download` temp files, and renames only verified temp files into place. Dart/Rust/alternate-builder metadata is reconciled to the live configured PIVX-hosted files. This is not release-complete until release-owner host/mirror approval, streaming/resume policy, and mobile Tor/interruption/low-storage tests are resolved.
- PIVX Sapling reorg handling now persists v1 `get_block_range` block hashes in encrypted sidecar state, compares the last 100 scanned shielded heights on resume when the node advertises block-hash support, rewinds notes/mined spend markers to the last matching height on mismatch, marks the global tree cursor untrusted after rewind, restores retained spendable notes into the native engine, and removes stale shielded receive history rows. This remains `Needs Verification` until tested against the v1 ElectrumX server/default nodes and manual restart/reorg scenarios.
- PIVX shielded spend-maturity diagnostics were tightened on 2026-06-11. `SaplingNoteStorage` now exposes a sanitized count-only eligibility summary for z-to-z note selection, and transaction construction logs counts for spendable, pending-confirmation, pending-spend, and missing-spending-data notes at the provisional 6-confirmation spend threshold. This remains open for release-owner confirmation-depth approval and simulator/physical-device blocked early-spend evidence.
- Read `15_electrumx_update_from_other_agent.md` for current server context: v1 support is reported under contract id `pivx.sapling.electrumx.v1`, primary capability method `blockchain.sapling.capabilities`, with v1 envelopes, global positions, anchor-bound witnesses, block hashes, rollback behavior, and 62 passing server tests. This remains incoming server evidence only until Cake Wallet is tested against deployed v1/default nodes.
- PIVX Sapling client-facing docs were aligned on 2026-06-01 to the reported ElectrumX v1 contract. `cw_pivx/SAPLING.md` and the `PIVXSaplingElectrumX` header now document `pivx.sapling.electrumx.v1`, `blockchain.sapling.capabilities`, v1 block-range envelopes, `block_hashes`, canonical global output positions, anchor-bound witness metadata, and structured failure semantics. Legacy method aliases are compatibility-only and are not default-node release evidence.
- Live default-node probe on 2026-05-31: the shipped Chainster PIVX defaults were reachable. `electrum02.chainster.org` has partial legacy Sapling RPC support (`get_block_range` returns a bare list with block hash/time and Sapling tx output fields; `get_witness` is registered enough to return `commitment not found` for a dummy commitment), but it lacks the v1 capability method, v1 envelopes/global positions, best-anchor, nullifier-status, and commitment-info methods. `electrum01.chainster.org` still returns `unknown method` for sampled Sapling methods. These defaults are not yet v1 Sapling-capable release evidence.
- Proving parameter metadata was reconciled on 2026-05-31. The configured PIVX-hosted URLs serve `sapling-spend.params` as `47,958,396` bytes with SHA-256 `8e48ffd23abb3a5fd9c5589204f32d9c31285a04b78096ba40a79b75677efc13`, and `sapling-output.params` as `3,592,860` bytes with SHA-256 `2f0ebbcbb9bb0bcffe95a397e7eba89c29eb4dde6191c339db88570e3f3fb0e4`. Dart constants, Rust prover verification, FFI `has_proving_params`, alternate-builder messaging, and `cw_pivx/SAPLING.md` now agree. This remains open for release-owner host/mirror approval and mobile Tor/interruption/low-storage tests.
- PIVX proving-parameter download handling was tightened further on 2026-06-01 for iOS simulator/device readiness. The active downloader now streams HTTP response chunks through Cake's proxy/Tor-aware `HttpClient` path into `.download` temp files while hashing incrementally, and cached-file verification also hashes from a file stream. Failed size/hash verification deletes temp files and never promotes partial downloads to final parameter paths. Focused local HTTP-server tests cover successful stream-to-temp/rename behavior and failed-verification temp cleanup.
- iOS simulator proving-parameter evidence was added on 2026-06-02. `xcrun simctl boot 9D573F2C-1B90-426E-B13D-7638D92CA790` booted the iPhone 16e simulator and `fvm flutter devices` detected it. The root app package graph now resolves `reown_yttrium` for `reown_walletkit`, so `fvm flutter build ios --simulator --no-pub` builds `build/ios/iphonesimulator/Runner.app`. A root app integration test, `integration_test/pivx_proving_params_download_test.dart`, downloads the real configured PIVX-hosted `sapling-spend.params` and `sapling-output.params` into simulator app storage, verifies size/hash through the streaming downloader, confirms no `.download` temp files remain, and passes on the booted iPhone 16e iOS 26.1 simulator. This still is not release-complete until release-owner host/mirror approval, Tor-device runs, interruption/resume or accepted retry policy, low-storage behavior, iOS background retry, and physical-device evidence pass.
- Native library loading/build hardening was added on 2026-05-31. Android plugin attachment now records missing/blocked `libcw_pivx_sapling.so` load state instead of crashing the app process, Dart FFI exposes a runtime self-test for library load, symbol lookup, version, and Core-aligned Sapling fee estimation, and Android build helpers now rebuild/fail when any required ABI artifact is missing or empty. On 2026-06-04, `tool/pivx_sapling_native_self_test.dart` was added as a repeatable command-line runner for that self-test, `sapling_ffi.dart` gained `PIVX_SAPLING_LIBRARY_PATH` artifact override support, and `cw_pivx/SAPLING.md` now documents the runner. Initial local evidence showed the checked-in macOS framework dylib loaded but failed fee parity (`native=25000`, `expected=1417000`), while freshly rebuilt Rust debug and release dylibs passed the Dart self-test. `bash cw_pivx/scripts/build_macos.sh` then rebuilt the checked-in universal macOS dylib/static lib/header from current Rust source, and the default macOS self-test now passes. This remains open for rebuilding checked-in/shipped iOS/Android artifacts from current Rust source, per-ABI hashes/version evidence, iOS packaging evidence, and physical-device self-test results.
- Backup policy hardening was added on 2026-05-31. Cake backup import/export now skips legacy plaintext PIVX Sapling sidecars, PIVX proving-parameter cache directories, and Sapling parameter temp downloads, while encrypted `.json.enc` Sapling sidecars remain eligible for Cake's encrypted app backup inclusion. This remains open for product/release-owner acceptance plus clean-device backup/export/restore evidence.
- PIVX restore hardening was added on 2026-05-31. Seed restore now exposes an optional block-height field; empty height preserves the safe import default of transparent genesis scan plus shielded Sapling activation scan, while entered height is persisted and used for transparent restore and the first shielded sync when no sidecar sync state exists. Shared Electrum address discovery now extends gap batches until a newly generated batch has no history, reducing transparent PIVX restore misses beyond one extra gap. Focused receive/change multi-gap regression coverage was added on 2026-06-01 and now passes under the repo-pinned FVM Flutter toolchain. This remains open for manual device/v1-node evidence.
- PIVX shielded seed-restore address reuse mitigation was added on 2026-06-01. After shielded sync, recovered note recipient bytes are compared with the first 1,000 derived Sapling payment addresses and `nextDiversifierIndex` is advanced past observed matches, avoiding immediate reuse of a previously used diversified shielded address without reconstructing old labels/current receive selection. Focused coverage now exercises advancement past the highest observed recipient plus scan-limit/no-backtracking behavior without requiring native Sapling FFI, and passes under the repo-pinned FVM Flutter toolchain. This remains open for manual seed-only restore/address-reuse evidence and release acceptance of the 1,000-index search limit.
- PIVX generated shielded receive-address handling was tightened on 2026-06-06. The receive list now accepts stored `diversifierIndex` values as int/num or legacy numeric strings and skips malformed rows without crashing, generated shielded addresses/labels/next receive index persist through sidecar reload, and wallet startup restores `currentShieldedAddress` from the highest stored diversifier index after shielded storage initializes. This keeps generated receive addresses current after restart instead of silently falling back to index 0. This remains open for simulator/device evidence that generating an additional shielded address, reopening the receive list, and restarting the app preserves selectable/current behavior.
- PIVX witness-source diagnostics were tightened on 2026-06-11. Accepted witness results now carry sanitized `source` metadata, and shielded transaction construction emits a `Witness source summary` showing whether the send used `anchor_bound` or `commitment_only_fallback`. This does not close the send gate; it makes the next simulator/device evidence decisive about whether `get_witness(commitment, anchor)` succeeded without fallback.
- PIVX shielded range-error classification was tightened on 2026-06-11. Incomplete `get_block_range` responses and structured retryable server/index states (`partial_index`, `index_not_ready`, `backend_timeout`) now sanitize to a distinct "complete Sapling block range not returned yet" message instead of the generic unsupported-block-scanning message. This is wallet-side observability only; post-broadcast shielded sync must still complete on simulator/device, or the server must return structured retryable errors that do not masquerade as complete ranges.
- PIVX send route warnings were tightened on 2026-06-11. The send view model now classifies `t-to-t`, `z-to-z`, disabled `t-to-z`, disabled `z-to-t`, mixed-output, and ambiguous-source states, and the send card shows an inline warning before construction when the selected PIVX source pool and destination imply an unsupported route. This mirrors the wallet fail-closed checks, but route gates remain open for manual device source-switch/send-all/transaction-creation evidence and product-approved wording.
- PIVX FFI memory hygiene was tightened on 2026-06-11. `SaplingKeys.fromSeed()` now overwrites the native `malloc<Uint8>` seed copy before freeing it, Dart-owned native UTF-8 inputs and short-lived Sapling byte buffers are wiped before `malloc.free()`, and focused coverage verifies the native byte-buffer and UTF-8 wipe helpers. Rust returned-allocation free paths now wipe library-owned strings and binary buffers before free, including both `cw_pivx_*` and legacy `pivx_*` free APIs, and `cw_pivx_free_buffer()` now releases its `libc::malloc()` allocation with `libc::free()`. `cw_pivx_build_shielded_tx()` now zeroizes Rust shielded-transaction result staging strings after copying them into the returned FFI buffer, including temporary txid/raw-transaction hex strings and error JSON/message strings. `SpendableNoteData` now zeroizes deserialized note-input strings on drop, including rseed/witness/nullifier/cmu/memo material. Rust `TransactionResult`, `TransparentUtxoData`, and `TransactionOptions` now zeroize tx/private-key/memo/address transfer strings on drop. This mitigates `PIVX-KEY-007` wallet-side, but `PIVX-MOB-004` remains open for deeper Rust-owned key/proving/transaction intermediates, allocator/platform residuals, and release-owner acceptance of the documented memory-hardening boundary.
- PIVX key/viewing-key recovery policy was tightened on 2026-06-01. PIVX Wallet Keys no longer exposes generic Electrum WIF/private/public/xPub rows, PIVX QR restore explicitly rejects private/view/spend/xpub/zpub key payloads with seed-plus-optional-restore-height guidance, and PIVX WIF restore now throws an explicit unsupported error after WIF validation. PIVX remains advertised as seed-only recovery until product/release owners approve migration guidance or a verified sweep/import/viewing-key workflow is implemented.
- PIVX recovery guidance UI was tightened on 2026-06-11. The PIVX Wallet Keys seed tab now shows a non-copyable warning that Cake Wallet PIVX recovery is seed-only, that users should keep seed phrase and restore height safe, that WIF/viewing-key/shielded-key restore is unsupported for PIVX, and that encrypted Cake backups may include PIVX shielded wallet state but do not replace the seed phrase. This remains open for product wording approval and device screenshot evidence.
- PIVX QR/URI/clipboard policy was tightened on 2026-06-11. The receive view model now detects selected PIVX Sapling receive addresses, shielded receive address/URI copy uses Cake's sensitive clipboard helper on supported mobile platforms, the shielded receive disclaimer names the sensitive-clipboard and no-memo posture, and PIVX payment URI parsing flags `memo`/`req-*` fields as unsupported so send scan/paste warns and does not silently populate Sapling memos. This remains open for product wording/policy approval, Android/iOS QR/copy/share evidence, and any auto-clear/app-switcher decision.
- PIVX secure-screen policy was tightened on 2026-06-11. `BasePage` now supports a route-lifetime native secure-screen scope that uses the existing `setIsAppSecure` method without changing the persisted global setting, and reference-counts nested sensitive routes. Wallet seed, Wallet Keys, and PIVX receive routes now force that scope. This remains open for product approval of the forced-screen list plus Android/iOS physical-device screenshot, screen-recording, and app-switcher evidence.
- PIVX shielded balance card UI was tightened on 2026-06-01. The shared dashboard second-balance card now gates Litecoin MWEB branding and Peg In/Peg Out controls to Litecoin/LTC rows only, so PIVX shielded confirmed/unconfirmed balance display no longer exposes Litecoin MWEB actions, Litecoin payment-request generation, or MWEB coin-type send arguments. This remains open for Android/iOS dashboard evidence and product acceptance that PIVX shield/deshield controls stay hidden until those routes are implemented and manually accepted.
- PIVX dashboard MWEB-gating coverage was tightened on 2026-06-02. The second-balance card's MWEB-control predicate is now exposed through a helper used by the widget build path, and focused coverage verifies it is true only for Litecoin/LTC and false for PIVX/PIVX plus non-Litecoin/LTC combinations. This remains open for Android/iOS dashboard screenshots/device evidence and product acceptance.
- PIVX send-page displayed balance was tightened on 2026-06-01. The send view's synchronous displayed-balance fallback now follows the selected PIVX source pool: transparent mode falls back to transparent available balance, and Sapling mode falls back to shielded `secondAvailable`, matching the pool-aware async send-balance/send-all helper. The send-balance refresh helper also recomputes for PIVX transparent/shielded source changes. This remains open for manual mixed transparent/shielded send-screen evidence.
- PIVX send-page displayed-balance fallback coverage was tightened on 2026-06-02. The PIVX source-pool fallback is now exposed through a helper used by `SendViewModel.balance`, and focused coverage verifies transparent/non-Sapling modes use transparent available balance, Sapling mode uses shielded `secondAvailable`, and missing PIVX balance data falls back to `0`. This remains open for manual mixed transparent/shielded send-screen evidence covering displayed balance, send-all, unsupported route errors, and final transaction creation.
- PIVX transparent balance malformed-response handling was tightened on 2026-06-01. `PivxWallet.fetchBalances()` now treats missing `confirmed`/`unconfirmed` Electrum balance fields as a malformed node response, sets lost-connection sync state, and returns the previous transparent balance with current shielded secondary balances instead of overwriting transparent balance with zero. On 2026-06-02, focused local regression coverage was added through the real PIVX wallet `fetchBalances()` path using stubbed Electrum responses for both missing fields. This remains open for malformed-node/device stale-balance UX evidence.
- PIVX shielded transaction-detail coverage was tightened on 2026-06-02. The details view model now exposes testable PIVX status/pool/route formatting helpers used by the row builder, and focused coverage pins pending shielded receive progress, spendable shielded receive, outgoing broadcast awaiting mined spend, mined-but-not-final outgoing shielded send, confirmed outgoing shielded send, negative-confirmation clamping, and PIVX pool/route labels. This remains open for restart/history persistence and manual Android/iOS detail evidence against Sapling-capable nodes.
- 2026-06-03 live default-node retests showed partial ElectrumX progress but default-node readiness remained blocked. Both Chainster defaults previously returned complete `pivx.sapling.electrumx.v1` capability metadata with `release_contract_ready: true`, and Cake Wallet iOS simulator progressed past capability probing, but shielded sync failed on v1 `get_block_range` because the Sapling commitment/global-position index was incomplete. Confirmed incomplete ranges included `2700500-2700599` at height `2700502`, txid `0c7d177ee5952f2a0d27fc25ae48cbf08ee0908fac9e5cc1b164b788c10fb608`, commitment `958a58f6c519d9f06fa3cd6c1bfbc8e3869d2b46d914443c139e0f2222bc9a21`, and `2700900-2700999` at height `2700908`, txid `3d31e42a939b429e24c8331e56dacd21942ba6358d77085f517bba3b9bc3cc61`, commitment `af164137ce42339ec52a7daaf3fc758459f49482e8a918b9a3ed472419f7e73b`. A later same-day probe found `electrum02.chainster.org:50002` and `:50001` refusing connections, while `electrum01.chainster.org:50001` still advertised complete v1 capabilities and `electrum01.chainster.org:50002` still returned `index_incomplete` for `2700500-2700599`. On 2026-06-04, `electrum01.chainster.org:50002` returned complete v1 capabilities and complete range envelopes for the previously failing ranges `2700500-2700599` (`block_count: 22`, `sapling_tx_count: 30`, `block_hash_count: 100`) and `2700900-2700999` (`block_count: 7`, `sapling_tx_count: 8`, `block_hash_count: 100`), plus an empty later range `2705000-2705099` (`empty: true`, `block_hash_count: 100`). Cake Wallet was reported showing "synchronizing shield data from block 2700500", which means it is past the v1 capability and initial range blockers on `electrum01`. Wallet-side logs are now height-aware enough to capture start/checkpoint/final range evidence on the next simulator/device run. Keep `PIVX-NET-002`/`PIVX-NET-003` open until Cake Wallet shielded sync completes on device/simulator and `electrum02` finishes reindexing/resyncing and passes the same probes.
- 2026-06-04 shielded-sync observability was added after the birthday-height retest showed generic Electrum logs were not enough to prove exact range heights. PIVX Sapling sync now logs sanitized height-only sync start, first completed range, coarse 10,000-block checkpoints, and final completed range. The ElectrumX block-range callback now passes both range start and range end, and focused coverage verifies failed ranges do not report completion while checkpoint logging remains bounded. This remains evidence support only; it does not close default-node readiness or shielded-sync completion.
- 2026-06-04 repeatable default-node probe tooling/evidence was added. `tool/pivx_sapling_default_node_probe.dart` probes the bundled PIVX Electrum nodes without wallet secrets and records server version, Sapling capability method, contract id, release-ready classification, network, activation height, global-position/block-hash support, required method presence, sampled `get_block_range` summaries, and safe live helper-method results. `fvm dart analyze tool/pivx_sapling_default_node_probe.dart` exits 0. `fvm dart run tool/pivx_sapling_default_node_probe.dart electrum01.chainster.org:50002:ssl electrum01.chainster.org:50001:plain` at `2026-06-04T00:51:09Z` showed both `electrum01` endpoints reachable with `ElectrumX 1.19.0`, `blockchain.sapling.capabilities`, contract `pivx.sapling.electrumx.v1`, `release_contract_ready: true`, mainnet activation `2700500`, block hashes, global output positions, all required v1 methods, and complete sampled range envelopes for `2700500-2700599`, `2700900-2700999`, and `2705000-2705099` with `height_count=100` and `block_hash_count=100`. An expanded run at `2026-06-04T01:08:20Z` found both `electrum01` endpoints still returning RPC `-32603` internal server error for `blockchain.sapling.get_best_anchor`; dummy nullifier status returns `spent=false`, and dummy commitment-info currently returns `null`, which still needs contract/client-shape confirmation. Cake Wallet now validates these live helper calls before accepting advertised v1 readiness and fails closed when they fail. A full bundled-default retest at `2026-06-04T01:23:31Z` confirmed both `electrum02` ports still refuse connections while reindexing, while `electrum01` still has complete v1 range evidence but `live_helper_methods_ready=false` because `get_best_anchor` continues to return RPC `-32603`. A targeted electrum01 retest at `2026-06-04T01:28:32Z` was unchanged: complete v1 metadata/ranges, `get_best_anchor` RPC `-32603`, dummy nullifier `spent=false`, dummy commitment-info `null`, and `live_helper_methods_ready=false`.
- 2026-06-04 birthday-height simulator retest: live `electrum01` tip was `5440392`, so scanning from activation `2700500` would require roughly 2.74 million historical blocks. The iPhone 16e simulator wallet metadata row `pivx_PIVX` was set to requested restore height `5440400`; after relaunch, Cake Wallet created encrypted sidecar `Documents/pivx_sapling_pivx_PIVX_mainnet.json.enc`, returned to `Synced`, and no sanitized PIVX Sapling failure appeared in observed logs. That run remains smoke evidence only because it happened before the new height-aware sync logs; rerun it with the new logs to prove exact shielded start/completion ranges. Wallet code was tightened so first shielded sync honors nonzero `walletInfo.restoreHeight` whenever Sapling storage is empty, and heightless fresh non-recovery wallets get a conservative creation-time birthday fallback. Focused `fvm dart analyze cw_pivx/lib/src/pivx_wallet.dart cw_pivx/test/cw_pivx_test.dart` exits 0 with only pre-existing `pivx_wallet.dart` deprecation infos, and `fvm flutter test cw_pivx/test/cw_pivx_test.dart --no-pub` passes all 14 focused PIVX tests.
- 2026-06-04 simulator sync/balance retest after the height-aware logs: `fvm flutter run -d 9D573F2C-1B90-426E-B13D-7638D92CA790 --no-pub` installed the current workspace build on the iPhone 16e iOS 26.1 simulator over the existing PIVX wallet data. The app restored spendable notes from encrypted storage, transparent sync reached `Synced`, and Sapling sync logged `Sync starting at 5440418; target 5440973`, first completed range `5440418-5440517`, final range `5440918-5440973`, `Sync complete: checked 4 outputs in 3 blocks with Sapling txs`, `Synced from 5440418 to 5440973`, and `Encrypted storage updated`. The dashboard showed transparent `0.0` PIVX and shielded `0.1` PIVX with no Litecoin MWEB branding/actions, but the top pill stayed stale at `56 Blocks Remaining` after backend Sapling completion. `PivxWalletBase.syncShielded()` now maps final Sapling progress back to `SyncedSyncStatus`; focused coverage was added. Hot restart on the same simulator synced the follow-up range `5440974-5440976`, emitted `SYNC_STATUS_CHANGE: Synced` after `0 blocks remaining`, and the dashboard showed `Shielded synced 5440976` while retaining transparent `0.0` and shielded `0.1`. Local screenshots are in `/private/tmp/pivx_iphone16e_after_sync.png`, `/private/tmp/pivx_iphone16e_post_final_sync.png`, and `/private/tmp/pivx_iphone16e_after_status_fix.png`. This improves simulator evidence but keeps the gate open for physical devices, non-capable-node UI states, `electrum01` helper success, and `electrum02` post-reindex evidence.
- 2026-06-04 post-server-update ElectrumX retest: `fvm dart run tool/pivx_sapling_default_node_probe.dart` at `2026-06-04T11:11:29Z` found all four shipped Chainster endpoints (`electrum01`/`electrum02`, SSL/plain) reachable with `ElectrumX 1.19.0`, `blockchain.sapling.capabilities`, contract `pivx.sapling.electrumx.v1`, `release_contract_ready: true`, complete sampled range envelopes with `block_hash_count=100`, successful `get_best_anchor`, dummy nullifier `spent=false`, structured dummy commitment-info `exists=false`, and `live_helper_methods_ready=true`. A fresh iPhone 16e simulator run then restored encrypted notes, reached generic `Synced`, loaded proving parameters, selected a shielded note, and fetched a spend anchor, but shielded send still failed at witness lookup: both `commitment_anchor` and `commitment_only` attempts failed and the user-facing error remained `canonical_witness_unavailable` / witness backend not implemented correctly. Treat the active server blocker as real-note canonical Sapling witness generation, not the older best-anchor helper failure. The same run also logged incomplete `get_block_range` for tip single-height range `5441053-5441053`.
- 2026-06-05 z-to-z bad-txn diagnostic pass: `PendingPivxShieldedTransaction` now logs only a sanitized broadcast transaction-shape summary (bytes, version/type, transparent vin/vout counts, Sapling optional-data presence, signed value balance, shielded spend/output counts, binding-signature presence, parse status) and maps PIVX Core `bad-txns-*` Sapling rejection strings to spend-proof/signature, output-proof, binding-signature, anchor/nullifier, nullifier, value-balance, activation, or invalid-form lanes. PIVX Core v5.6.1 source spot-check found pure z-to-z empty-transparent transactions are allowed when Sapling data is present, and that spend/output/binding bad-txn strings come from Core's recomputed Sapling validation path. No definitive serialization/sighash fix was identified; if simulator z-to-z gets past witness fetch and then receives `bad-txns-*`, keep `PIVX-SEND-007` open and compare Cake/librustpivx raw tx, txid, sighash preimage/hash, proofs, spend auth signatures, and binding signature against PIVX Core v5.6.1 or the selected Core build.
- 2026-06-05 simulator z-to-z retest and sync-cadence update: iPhone 16e iOS 26.1 simulator z-to-z send loaded proving parameters, selected a shielded note, fetched a spend anchor, attempted anchor-bound witness first, failed that attempt with sanitized `witness_lookup_failed`, accepted commitment-only witness fallback, built/proved through FFI, and broadcast returned a string txid. Sanitized broadcast shape was `bytes=2365 version=3 type=0 vin=0 vout=0 sapling=present value_balance=2365000 shielded_spends=1 shielded_outputs=2 binding_sig=present parse=ok`; no `bad-txns-*` rejection appeared in the captured log. Post-broadcast shielded sync failed and the dashboard showed `Shielded sync failed`; screenshot captured at `/private/tmp/pivx_z_to_z_after_broadcast.png`. The wallet now logs sanitized post-broadcast shield-sync failures. PIVX transparent sync is Electrum subscription-driven, while shielded notes now also subscribe to block headers and trigger shielded sync at most once per `PivxNetwork.targetBlockTime` (`60` seconds), matching PIVX's target block interval. The default-node probe now includes current-tip and previous-tip single-height range checks; a 2026-06-05T11:08:52Z run found both `electrum01` endpoints green but `electrum02:50002` timing out on dummy commitment-info, sampled ranges, and current-tip lookup, and `electrum02:50001` timing out at `server.version`. The probe now emits `endpoint_probe_ready` plus `degraded_reasons`; fresh bundled runs at 2026-06-05T11:16:50Z and 2026-06-05T11:22:31Z found all four Chainster endpoints reachable and ready with complete sampled/current-tip/previous-tip envelopes and `degraded_reasons=none`. Keep default-node readiness open for device/node-list evidence and keep send readiness open for anchor-bound real-note witness and post-broadcast shielded sync.
- The local Flutter test blocker was toolchain selection, not package resolution for the touched tests: bare `flutter` is Homebrew Flutter 3.38.3/Dart 3.10.1, while the repo is pinned to FVM Flutter 3.32.0/Dart 3.8.0. Use `fvm flutter ...` and `fvm dart ...` for this branch unless the project pin changes.
- On 2026-06-01, Dart fee policy was corrected so `PivxFeePolicy.feeForSize()` keeps the 10,000 zatoshi min-relay floor for default relay fee calculations but does not clamp `dustRelayFeePerKb` calculations. This restores the Core-derived transparent dust calculation of `5,460` while keeping the default 182-byte relay fee expectation at `10,000`.
- 2026-06-12 real-commitment witness probe and server-side anchor fix: `tool/pivx_sapling_default_node_probe.dart` now validates anchor-bound and commitment-only `get_witness` against a real on-chain commitment with the wallet's acceptance rules, and flags structured `available=false` helper responses as unavailable. Live results: both `electrum01` endpoints serve `real_witness_anchor_bound: success source=anchor_bound` (32 canonical nodes, matching anchor/height/commitment/position) — the wallet's anchor-bound criterion is now demonstrably servable; both `electrum02` endpoints time out on anchor-bound witness because their canonical best-anchor helper exceeds the 8s timeout at the live 44,934-output tree size and falls back to a legacy-encoded anchor that can never bind. Server fixes were implemented in the sibling `electrum` repo working tree (frontier-based historical-anchor search ~0.3s current/~6s worst-case at live scale, rayon-parallel hashing, stable last-leaf anchor heights, tree-content cache keys, fail-closed `get_best_anchor` with structured `canonical_anchor_unavailable`, env-test pollution fix); all 38 server Sapling tests pass and changes are uncommitted pending release-owner review/deploy. See `14_electrumx_followups.md` 2026-06-12 entries. Today's probe also reconfirmed complete current-tip/previous-tip single-height range envelopes on all four endpoints, so the post-broadcast incomplete-tip-range symptom has no remaining server-side evidence; the remaining post-broadcast sync gate is simulator/device evidence only.
- 2026-06-12 wallet test-suite repair: the full `fvm flutter test cw_pivx/test --no-pub` run had one failure — the lexical log-redaction guard flagged five reviewed witness-diagnostic log lines that interpolate only counts/enum codes/booleans. `pivx_log_redaction_test.dart` now carries an explicit reviewed-sanitized-statement allowlist keyed on exact statement text (any edit re-triggers review), and all 102 PIVX package tests pass.
- 2026-06-12 default-node anchor-bound witness closure: after two same-day server redeploys by the release owner, the real-commitment probe found ALL FOUR bundled Chainster endpoints serving `real_witness_anchor_bound: success source=anchor_bound` with canonical best anchors (`root`, `tree_size`), stable last-leaf anchor heights (correctly tracking a live tree growth from 44,934 to 44,936 outputs), and complete tip single-height envelopes. The server also gained an on-disk witness tree-state cache (warm `get_witness` ~73ms at live scale, automatic `pivx_sapling_witness_state.bin` in the ElectrumX DB dir) after the release owner reported 2.5s slow-log lines. The active ElectrumX send blocker is closed server-side; remaining acceptance is a simulator/device z-to-z send logging `Witness source summary: anchor_bound` plus successful post-broadcast shielded sync. Server changes live uncommitted in the sibling `electrum` repo working tree (release owner commits; no AI attribution lines in commit messages per owner instruction).
- 2026-06-12 z-to-t (deshield) route implementation: the PIVX z-to-t route is now implemented end to end and no longer fail-closed. Rust: `TransparentOutput` with strict base58check P2PKH/P2SH decoding for mainnet (30/13) and testnet (139/19) prefixes, Core-exact `vout` serialization, sighash `hashOutputs` over real vouts plus the Core empty-shielded-leg zero-bytes rule (z-to-z legs byte-identical to the proven path), `validate_route_transaction_amounts` with the 5,460 transparent dust floor, `build_route_transaction` with shielded-only change, and FFI destination detection in `cw_pivx_build_shielded_tx` (Sapling bech32 → z-to-z; base58 transparent → z-to-t; memo rejected for transparent destinations). Dart: the Sapling builder routes by destination type with route-aware dust floors and fee plans (`planShieldedSpend`/`selectNotesForAmount` gained `transparentDestination`, 0/1 sapling destination outputs + 1 transparent output), `PivxWallet.createTransaction` routes shielded-source/transparent-destination sends through the shielded builder, `_shieldedSendAllAmount` is destination-aware, `deshieldFunds` is implemented, and the send view model classifies `shieldedToTransparent` as a supported route (warning removed). Verification: 41 Rust tests pass (new coverage: base58check decode/corruption/wrong-network, P2PKH/P2SH scripts, vout serialization, transparent dust validation), all 105 cw_pivx Dart tests pass (new z-to-t fee-plan coverage), all 10 send VM route tests pass (z-to-t flipped to supported), focused analyzers exit clean. Open: simulator/device z-to-t broadcast/mempool-acceptance evidence, native artifact rebuilds for iOS/Android (`bash cw_pivx/scripts/build_ios.sh` / `build_android.sh`) before device testing, shielded history/details route labeling for z-to-t rows, and t-to-z (shield) which remains fail-closed pending transparent-input signing (secp256k1 + per-input ZIP-243-style sighash legs verified against PIVX Core interpreter.cpp lines 1119-1280, local checkout `/Users/liquid/Projects/PIVX`).
- 2026-06-12 t-to-z (shield) route implementation: the PIVX t-to-z route is now implemented end to end, completing all four route types in code (t-to-t, z-to-z, z-to-t, t-to-z). Rust: `TransparentInput` parses UTXO txid/vout/value/script/private-key, rejects non-P2PKH scripts, and fails closed when the derived compressed pubkey hash does not match the script; the sighash was refactored into a shared common-state (`sighash_common_state` + `finalize_binding_sighash` + `finalize_input_sighash`) so the binding signature and every transparent input ECDSA signature commit to the real vin/vout legs per PIVX Core `interpreter.cpp` `SignatureHash` (prevout + scriptCode + amount + nSequence + locktime + SIGHASH_ALL, branch id 0); `build_shield_transaction` builds an outputs-only Sapling bundle (empty-tree anchor, negative value balance), requires exact amount balance, signs inputs with secp256k1 DER + SIGHASH_ALL P2PKH scriptSigs, and serializes vin/vout/sapData; new FFI `cw_pivx_build_shield_tx` takes utxos JSON + sapling destination + optional transparent change address/amount. Dart: `sapling_ffi.dart` binds `buildShieldTransaction`, the builder wrapper exposes `buildShieldTransaction` plus a `planShieldSpend` fee/change planner (transparent dust change absorbed into fee), `PivxNetwork.p2pkhScriptPubKeyHex` builds scripts from addresses, and `PivxWallet` now routes transparent-source/shielded-destination sends through `_buildShieldTransactionResult` (confirmed P2PKH UTXO selection largest-first, per-address ECPrivate keys from `hd`/`sideHd` + record index, transparent change to the current address, send-all support) and implements `shieldFunds`; the send view model classifies `transparentToShielded` as supported. Verification: all 41 Rust tests pass, all 107 cw_pivx tests pass (new t-to-z plan coverage), all 10 send VM tests pass, focused analyzers report only pre-existing infos. Open: simulator/device t-to-z broadcast/mempool acceptance, Core-vector comparison if `bad-txns-*` appears (per-input sighash is new consensus surface), iOS/Android artifact rebuilds, history/details route labels for shield/deshield rows, and release-owner review of the transparent change-address policy (currently the current receive address, not a fresh change address).
- Server-side ElectrumX work has a dedicated follow-up document: `14_electrumx_followups.md`.

Verification so far:

- `dart analyze` on touched Dart files exits 0, with pre-existing deprecation infos in `pivx_wallet.dart` for `snp.mnemonic`, `snp.xpub`, and `snp.passphrase`.
- `dart analyze cw_pivx/lib/src/sapling/sapling_constants.dart cw_pivx/lib/src/sapling/sapling_transaction_builder.dart cw_pivx/lib/src/sapling/sapling_factories.dart cw_pivx/lib/src/pivx_wallet.dart lib/pivx/pivx.dart lib/pivx/cw_pivx.dart lib/view_model/dashboard/dashboard_view_model.dart lib/src/screens/dashboard/widgets/sync_indicator.dart lib/src/screens/settings/connection_sync_page.dart lib/src/widgets/standard_list_status_row.dart lib/view_model/dashboard/transaction_list_item.dart lib/view_model/node_list/node_create_or_edit_view_model.dart lib/src/screens/nodes/widgets/node_form.dart cw_pivx/test/pivx_fee_policy_test.dart` exits 0, with only the same pre-existing `pivx_wallet.dart` deprecation infos.
- `cargo test --manifest-path cw_pivx/rust/Cargo.toml test_ffi_estimate_fee` passes, with pre-existing Rust warnings.
- `dart analyze cw_pivx/lib/src/sapling/pivx_sapling_electrumx.dart cw_pivx/lib/src/sapling/sapling_constants.dart cw_core/lib/node.dart cw_core/lib/node.g.dart lib/view_model/node_list/node_create_or_edit_view_model.dart lib/src/screens/nodes/widgets/node_list_row.dart lib/view_model/transaction_details_view_model.dart cw_pivx/lib/src/pivx_wallet.dart` exits 0, with one pre-existing `cw_core/lib/node.dart` deprecation info and the same pre-existing `pivx_wallet.dart` deprecation infos.
- `dart analyze cw_pivx/lib/src/sapling/sapling_factories.dart cw_pivx/lib/src/sapling/sapling_constants.dart cw_pivx/lib/src/pivx_wallet.dart` exits 0 after the proving-parameter downloader hardening, with only the same pre-existing `pivx_wallet.dart` deprecation infos.
- `dart analyze cw_pivx/lib/src/sapling/sapling_constants.dart cw_pivx/lib/src/sapling/sapling_factories.dart cw_pivx/lib/src/pivx_wallet.dart cw_pivx/test/pivx_fee_policy_test.dart` exits 0 after applying the PIVX Core factor-100 Sapling fee policy, with only the same pre-existing `pivx_wallet.dart` deprecation infos.
- `dart analyze cw_pivx/lib/src/sapling/sapling_constants.dart cw_pivx/lib/src/sapling/sapling_factories.dart cw_pivx/lib/src/pivx_wallet.dart cw_pivx/test/pivx_fee_policy_test.dart` exits 0 after applying the exact PIVX Core transparent/shielded dust thresholds, with only the same pre-existing `pivx_wallet.dart` deprecation infos.
- `dart analyze cw_pivx/lib/src/sapling/sapling_note_storage.dart cw_pivx/lib/src/sapling/pivx_sapling_electrumx.dart cw_pivx/lib/src/sapling/sapling_factories.dart cw_pivx/lib/src/pivx_wallet.dart cw_pivx/test/pivx_sapling_electrumx_test.dart cw_pivx/test/sapling_note_storage_test.dart` exits 0 after adding wallet-side Sapling block-hash/reorg rewind handling, with only the same pre-existing `pivx_wallet.dart` deprecation infos.
- `dart analyze cw_pivx/lib/src/sapling/sapling_constants.dart cw_pivx/lib/src/sapling/native_sapling_transaction_builder.dart cw_pivx/lib/src/sapling/sapling_factories.dart cw_pivx/test/pivx_fee_policy_test.dart` exits 0 after proving-parameter metadata reconciliation.
- `dart analyze cw_pivx/lib/src/sapling/sapling_ffi.dart` exits 0 after native-library self-test/load-state hardening.
- `fvm dart analyze cw_pivx/lib/src/sapling/sapling_ffi.dart tool/pivx_sapling_native_self_test.dart` exits 0 after adding the native self-test runner and artifact override.
- Initial `fvm dart run tool/pivx_sapling_native_self_test.dart` on macOS loaded the checked-in `cw_pivx/macos/Frameworks/libcw_pivx_sapling.dylib` but failed fee parity with `native=25000 expected=1417000`, proving the packaged macOS artifact was stale before rebuild.
- `cargo test --manifest-path cw_pivx/rust/Cargo.toml test_ffi_estimate_fee` passes with pre-existing Rust warnings.
- `cargo build --manifest-path cw_pivx/rust/Cargo.toml` followed by `PIVX_SAPLING_LIBRARY_PATH=/Users/liquid/Projects/cake_wallet/cw_pivx/rust/target/debug/libcw_pivx_sapling.dylib fvm dart run tool/pivx_sapling_native_self_test.dart` passes after rebuilding the debug dylib.
- `cargo build --manifest-path cw_pivx/rust/Cargo.toml --release` followed by `PIVX_SAPLING_LIBRARY_PATH=/Users/liquid/Projects/cake_wallet/cw_pivx/rust/target/release/libcw_pivx_sapling.dylib fvm dart run tool/pivx_sapling_native_self_test.dart` passes after rebuilding the release dylib.
- `bash cw_pivx/scripts/build_macos.sh` rebuilt the checked-in universal macOS artifacts. `fvm dart run tool/pivx_sapling_native_self_test.dart` now passes against `cw_pivx/macos/Frameworks/libcw_pivx_sapling.dylib` with `version=0.1.0`, `loaded=true`, `symbols_ready=true`, and `fee_matches_policy=true`. SHA-256 hashes: dylib `40e4ce2467f154c313adb0296d882ca9363c1cde1149401235899faee5e2b692`, static lib `b5b1129a23e50f73dec34f18a816321e29d3c418e641367f0cc698df8ddbea7a`, header `4e521cfcf050679fcf57e887a8ff03cd712daa159989ebd8a57c241db9b1e3c8`.
- `bash cw_pivx/scripts/build_ios.sh` rebuilt local iOS Sapling artifacts from current Rust source at checkout commit `e575783afd869f4d3292216106bea8aba2bbe0d8` with uncommitted PIVX audit/native-tooling changes still present. SHA-256 hashes: `ios-arm64/libcw_pivx_sapling.a` `5e17451c72c045598d69edfb68c0f0afb7ce34e817371162d3647a846275a9f4`, `ios-arm64_x86_64-simulator/libcw_pivx_sapling.a` `09b7c2368846648043a5083946f1c6b9c830001f059a80990196bca0fc2bcf35`, header `4e521cfcf050679fcf57e887a8ff03cd712daa159989ebd8a57c241db9b1e3c8`, and `Info.plist` `bc358716655e7a67cf89e89bc89945ae689237dcdfa131e2009635ce16cd052b`. The generated XCFramework directory is ignored by `cw_pivx/ios/.gitignore`, and the build emitted deployment-target linker warnings for bundled `secp256k1` objects built for iOS/iOS-simulator 26.1 against linked targets 10.0/14.0.
- `bash cw_pivx/scripts/build_android.sh` rebuilt all four local Android Sapling ABI artifacts from current Rust source and verified non-empty outputs. SHA-256 hashes: `arm64-v8a` `fb0880f8d9f66c6d712b75927f62e0ab10b835802537aea151947fa6aea88960`, `armeabi-v7a` `f7bb8991f3111865393497c0f46dcb315d0be456d808e37abaaa96e7900346d8`, `x86_64` `9ccd98ec7055045e3628495423bd3c243d6e45e8b5a28e264cb5265cbd4c37a6`, and `x86` `4f5bf0be57e72a462dcfdebf7d577556e914e4c7025e022b0a54be91d57829dc`.
- `bash -n scripts/build_android_macos.sh`, `bash -n cw_pivx/scripts/build_android.sh`, and `git diff --check -- cw_pivx/lib/src/sapling/sapling_ffi.dart cw_pivx/android/src/main/kotlin/com/cakewallet/cw_pivx/CwPivxPlugin.kt scripts/build_android_macos.sh cw_pivx/scripts/build_android.sh` exit 0 after Android ABI build checks were hardened.
- `dart analyze lib/core/backup_service.dart lib/core/backup_service_v3.dart` exits 0 after backup inclusion/exclusion hardening, with one pre-existing `encryptionKey` deprecation info.
- `git diff --check -- lib/core/backup_service.dart lib/core/backup_service_v3.dart` exits 0 after backup inclusion/exclusion hardening.
- `dart analyze cw_bitcoin/lib/electrum_wallet_addresses.dart` exits 0 after fixing multi-gap transparent address discovery.
- Focused `dart analyze lib/view_model/wallet_restore_view_model.dart lib/src/screens/restore/wallet_restore_page.dart lib/pivx/pivx.dart lib/pivx/cw_pivx.dart cw_pivx/lib/src/pivx_wallet_creation_credentials.dart cw_pivx/lib/src/pivx_wallet.dart cw_pivx/test/cw_pivx_test.dart` reports only pre-existing `WalletRestorePage` mutability/deprecation infos and pre-existing `pivx_wallet.dart` deprecation infos after PIVX restore-height wiring.
- `fvm flutter test cw_pivx/test/cw_pivx_test.dart --no-pub` passes after adding the restore test DB harness and running under the repo-pinned Flutter 3.32.0 toolchain.
- Focused `dart analyze cw_pivx/lib/src/pivx_wallet.dart cw_pivx/lib/src/sapling/sapling_note_storage.dart cw_pivx/test/sapling_note_storage_test.dart cw_pivx/test/cw_pivx_test.dart` exits 0 after the shielded seed-restore address-reuse mitigation, with only pre-existing `pivx_wallet.dart` snapshot deprecation infos.
- `fvm flutter test cw_pivx/test/sapling_note_storage_test.dart --no-pub` passes under the repo-pinned Flutter 3.32.0 toolchain.
- Focused `dart analyze cw_pivx/lib/src/pivx_wallet.dart cw_pivx/test/cw_pivx_test.dart` exits 0 after adding multi-gap transparent restore and shielded diversified-recipient advancement tests, with only the same pre-existing `pivx_wallet.dart` deprecation infos.
- `fvm dart analyze cw_pivx/lib/src/sapling/sapling_constants.dart cw_pivx/lib/src/pivx_wallet.dart cw_pivx/test/cw_pivx_test.dart cw_pivx/test/pivx_fee_policy_test.dart cw_pivx/test/pivx_sapling_electrumx_test.dart cw_pivx/test/sapling_note_storage_test.dart` exits 0 with only the same pre-existing `pivx_wallet.dart` deprecation infos.
- `fvm flutter test cw_pivx/test --no-pub` passes all 65 PIVX tests after using the pinned FVM toolchain and fixing the Dart dust-relay fee calculation.
- `fvm dart analyze cw_pivx/lib/src/pivx_wallet_service.dart lib/view_model/wallet_keys_view_model.dart lib/view_model/restore/wallet_restore_from_qr_code.dart` exits 0 after the PIVX seed-only/key-restore fail-closed policy update.
- `fvm flutter test cw_pivx/test --no-pub` still passes all 65 PIVX tests after the PIVX recovery-policy update. The first sandboxed attempt failed because FVM needed to write its SDK cache outside the workspace; rerunning with approval succeeded.
- `fvm dart analyze lib/view_model/wallet_keys_view_model.dart lib/src/screens/wallet_keys/wallet_keys_page.dart` exits 0 after adding the PIVX seed-only recovery notice, with only pre-existing `wallet_keys_page.dart` `withOpacity` deprecation infos.
- `fvm dart analyze lib/src/screens/dashboard/pages/balance/balance_row_widget.dart` exits 0 after gating Litecoin MWEB balance-card branding/actions away from PIVX shielded balance rows. The first sandboxed attempt failed because FVM needed to write its SDK cache outside the workspace; rerunning with approval succeeded.
- `fvm dart analyze lib/src/screens/dashboard/pages/balance/balance_row_widget.dart test/src/screens/dashboard/pages/balance/balance_row_widget_test.dart` exits 0 after adding focused dashboard MWEB-gating coverage. `fvm flutter test test/src/screens/dashboard/pages/balance/balance_row_widget_test.dart --no-pub` passes all 3 focused MWEB-gating tests.
- `fvm dart analyze lib/view_model/send/send_view_model.dart` exits 0 after making the PIVX send-page displayed-balance fallback source-pool aware. The first sandboxed attempt failed because FVM needed to write its SDK cache outside the workspace; rerunning with approval succeeded.
- `fvm dart analyze lib/view_model/send/send_view_model.dart test/view_model/pivx_send_view_model_test.dart` exits 0 after adding focused PIVX send displayed-balance fallback coverage. `fvm flutter test test/view_model/pivx_send_view_model_test.dart --no-pub` passes all 3 focused send fallback tests.
- `fvm dart analyze cw_pivx/lib/src/pivx_wallet.dart` exits 0 after adding malformed transparent balance response handling, with only the same pre-existing snapshot deprecation infos. `fvm flutter test cw_pivx/test --no-pub` passes all 65 PIVX tests after the change.
- `fvm dart analyze cw_pivx/lib/src/pivx_wallet.dart cw_pivx/test/cw_pivx_test.dart` exits 0 after adding focused malformed transparent balance response regression coverage, with only the same pre-existing `pivx_wallet.dart` snapshot deprecation infos. `fvm flutter test cw_pivx/test/cw_pivx_test.dart --no-pub` passes all 11 focused PIVX restore/balance tests, and `fvm flutter test cw_pivx/test --no-pub` passes all 76 local PIVX tests.
- `fvm dart analyze lib/view_model/transaction_details_view_model.dart test/view_model/pivx_transaction_details_view_model_test.dart` exits 0 after adding focused PIVX shielded transaction-detail formatting coverage. `fvm flutter test test/view_model/pivx_transaction_details_view_model_test.dart --no-pub` passes all 7 focused detail-formatting tests.
- `fvm dart analyze cw_pivx/lib/src/sapling/pivx_sapling_electrumx.dart` exits 0 after aligning the client-facing Sapling ElectrumX v1 contract docs/header to `15_electrumx_update_from_other_agent.md`.
- `cargo test --manifest-path cw_pivx/rust/Cargo.toml test_activation_heights` passes after aligning testnet activation to `201`, with pre-existing Rust warnings for unused `activation` and `key_handle`.
- `cargo test --manifest-path cw_pivx/rust/Cargo.toml test_ffi_estimate_fee` passes after updating Rust FFI fee estimation to the factor-100 Sapling policy, with the same pre-existing Rust warnings.
- `cargo test --manifest-path cw_pivx/rust/Cargo.toml test_shielded_dust_threshold` passes after aligning Rust builder validation to the PIVX Core shielded dust threshold, with the same pre-existing Rust warnings.
- `cargo test --manifest-path cw_pivx/rust/Cargo.toml test_expected_param_hashes_are_canonical_sha256`, `test_ffi_estimate_fee`, and `test_shielded_dust_threshold` pass after proving-parameter metadata reconciliation, with the same pre-existing Rust warnings.
- `fvm dart analyze cw_pivx/lib/src/sapling/sapling_factories.dart cw_pivx/test/pivx_fee_policy_test.dart` exits 0 after adding streaming proving-parameter downloads and local stream/temp/failure-cleanup coverage.
- `fvm flutter test cw_pivx/test/pivx_fee_policy_test.dart --no-pub` passes all 9 focused fee/parameter tests after the streaming proving-parameter downloader update.
- `fvm flutter test cw_pivx/test --no-pub` passes all 67 local PIVX tests after the streaming proving-parameter downloader update.
- `fvm dart analyze integration_test/pivx_proving_params_download_test.dart cw_pivx/lib/src/sapling/sapling_factories.dart` exits 0 after adding the root PIVX proving-parameter integration test.
- `fvm dart analyze cw_pivx/lib/src/sapling/pivx_sapling_electrumx.dart cw_pivx/test/pivx_sapling_electrumx_test.dart lib/src/screens/nodes/widgets/node_list_row.dart lib/view_model/node_list/node_create_or_edit_view_model.dart lib/core/node_switching_service.dart` exits 0 after tightening v1 node-readiness classification.
- `fvm flutter test cw_pivx/test/pivx_sapling_electrumx_test.dart --no-pub` passes all 20 focused ElectrumX capability/envelope/witness/helper-validation tests, including complete v1, incomplete v1, legacy fallback, null malformed capability responses, and advertised-v1 rejection when live best-anchor validation fails.
- `xcrun simctl boot 9D573F2C-1B90-426E-B13D-7638D92CA790` and `fvm flutter devices` confirm the iPhone 16e iOS 26.1 simulator is available. `fvm flutter build ios --simulator --no-pub` now succeeds and builds `build/ios/iphonesimulator/Runner.app`.
- `fvm flutter test integration_test/pivx_proving_params_download_test.dart -d 9D573F2C-1B90-426E-B13D-7638D92CA790 --no-pub` passes on the iPhone 16e iOS 26.1 simulator after downloading and verifying the real configured PIVX Sapling parameter files.
- `fvm dart analyze cw_pivx/lib/src/sapling/pivx_sapling_electrumx.dart cw_pivx/lib/src/sapling/sapling_factories.dart cw_pivx/test/pivx_sapling_electrumx_test.dart` exits 0 after adding height-aware shielded-sync logging.
- `fvm flutter test cw_pivx/test/pivx_sapling_electrumx_test.dart --no-pub` passes all 20 focused ElectrumX/sync-observability/helper-validation tests after adding range-start callbacks, checkpoint-selection coverage, and advertised-v1 live helper fail-closed coverage.
- `fvm dart analyze cw_pivx/lib/src/sapling/pivx_sapling_electrumx.dart cw_pivx/test/pivx_sapling_electrumx_test.dart tool/pivx_sapling_default_node_probe.dart` exits 0 after adding the repeatable PIVX default-node probe and live helper validation.
- `fvm dart run tool/pivx_sapling_default_node_probe.dart electrum01.chainster.org:50002:ssl electrum01.chainster.org:50001:plain` at `2026-06-04T01:28:32Z` records complete v1 capability/range evidence for both `electrum01` endpoints, but also records `live_helper_methods_ready: false` because `blockchain.sapling.get_best_anchor` returns RPC `-32603` internal server error. `fvm dart run tool/pivx_sapling_default_node_probe.dart` at `2026-06-04T01:23:31Z` records both `electrum02` default ports still refusing connections and pending reindex completion.
- `fvm flutter run -d 9D573F2C-1B90-426E-B13D-7638D92CA790 --no-pub` on the iPhone 16e iOS 26.1 simulator restores the existing PIVX wallet, completes height-aware Sapling sync from `5440418` to `5440973`, updates encrypted storage, and displays transparent `0.0` plus shielded `0.1`. The first run exposed a stale `56 Blocks Remaining` dashboard pill after Sapling completion; after the status fix, hot restart syncs `5440974-5440976`, logs `SYNC_STATUS_CHANGE: Synced`, and the dashboard shows `Shielded synced 5440976`.
- `fvm dart analyze cw_pivx/lib/src/pivx_wallet.dart cw_pivx/test/cw_pivx_test.dart` reports only the pre-existing `pivx_wallet.dart` deprecation infos after the shielded final-progress status fix.
- `fvm flutter test cw_pivx/test/cw_pivx_test.dart --no-pub` passes all 15 focused PIVX tests after adding the stale shielded-progress regression.
- `fvm dart analyze cw_pivx/lib/src/pending_pivx_shielded_transaction.dart cw_pivx/test/cw_pivx_test.dart` exits 0 after adding sanitized shielded broadcast diagnostics and Core bad-txn rejection mapping.
- `fvm flutter test cw_pivx/test/cw_pivx_test.dart --no-pub` passes all 17 focused PIVX tests after adding shielded broadcast diagnostic coverage. The output still includes pre-existing invalid-bech32 logs from restore-discovery tests, but the tests pass.
- `fvm dart analyze cw_pivx/lib/src/pivx_wallet.dart cw_pivx/test/cw_pivx_test.dart` reports only the pre-existing `pivx_wallet.dart` deprecation infos after adding the PIVX 60-second shielded header-sync cadence and sanitized post-broadcast sync failure logging.
- `fvm flutter test cw_pivx/test/cw_pivx_test.dart --no-pub` passes all 18 focused PIVX tests after adding shielded header-sync cadence coverage.
- `fvm dart analyze tool/pivx_sapling_default_node_probe.dart` exits 0 after adding endpoint-level readiness/degraded-reason reporting to the default-node probe. `fvm dart run tool/pivx_sapling_default_node_probe.dart` at `2026-06-05T11:16:50Z` and again after reported node updates at `2026-06-05T11:22:31Z` finds all four bundled Chainster endpoints reachable with v1 metadata, live helper success, complete sampled ranges, complete current-tip/previous-tip single-height ranges, `endpoint_probe_ready=true`, and `degraded_reasons=none`.
- `fvm dart analyze cw_pivx/lib/src/pivx_wallet.dart cw_pivx/test/cw_pivx_test.dart cw_pivx/test/sapling_note_storage_test.dart lib/view_model/wallet_address_list/wallet_address_list_view_model.dart test/view_model/pivx_wallet_address_list_view_model_test.dart` exits 0 after the generated shielded receive-address/current-address fix, with only pre-existing `pivx_wallet.dart` deprecation infos. `fvm flutter test cw_pivx/test/cw_pivx_test.dart cw_pivx/test/sapling_note_storage_test.dart test/view_model/pivx_wallet_address_list_view_model_test.dart --no-pub` passes all 46 focused tests.
- `fvm dart analyze cw_pivx/lib/src/sapling/pivx_sapling_electrumx.dart cw_pivx/lib/src/sapling/sapling_factories.dart cw_pivx/test/pivx_sapling_electrumx_test.dart` exits 0 after adding sanitized witness-source metadata. `fvm flutter test cw_pivx/test/pivx_sapling_electrumx_test.dart --no-pub` passes all 26 focused ElectrumX tests.
- `fvm dart analyze cw_pivx/lib/src/sapling/sapling_note_storage.dart cw_pivx/lib/src/sapling/sapling_factories.dart cw_pivx/test/sapling_note_storage_test.dart` exits 0 after adding count-only shielded spend eligibility diagnostics. `fvm flutter test cw_pivx/test/sapling_note_storage_test.dart --no-pub` passes all 24 focused storage tests.
- `fvm dart analyze lib/view_model/wallet_address_list/wallet_address_list_view_model.dart lib/src/screens/receive/widgets/qr_widget.dart lib/src/screens/receive/receive_page.dart lib/utils/payment_request.dart lib/src/screens/send/widgets/send_card.dart test/utils/payment_request_test.dart` exits 0 after adding PIVX shielded receive sensitive-clipboard handling and unsupported PIVX URI field warnings. `fvm flutter test test/utils/payment_request_test.dart --no-pub` passes all 9 focused payment URI tests.
- `fvm dart analyze lib/utils/secure_screen.dart lib/src/screens/base_page.dart lib/src/screens/seed/wallet_seed_page.dart lib/src/screens/wallet_keys/wallet_keys_page.dart lib/src/screens/receive/receive_page.dart test/utils/secure_screen_test.dart` exits 0 after adding the route-lifetime secure-screen scope, with only pre-existing `wallet_keys_page.dart` `withOpacity` deprecation infos. `fvm flutter test test/utils/secure_screen_test.dart --no-pub` passes all 4 focused secure-scope tests.
- Bare `flutter test ... --no-pub` still uses the wrong Homebrew Flutter 3.38.3/Dart 3.10.1 toolchain in this local environment and fails in Flutter semantics APIs (`SemanticsRole.searchBox`, `SemanticsData.elevation`, non-exhaustive `SemanticsRole.complementary`). This is superseded for branch verification by the passing `fvm flutter` runs above.

Primary next implementation themes:

- Close Critical/High blockers in `11_findings_register.md` for enabled PIVX features.
- Continue with ElectrumX anchor-bound real-note witness backend retests, post-broadcast shielded sync failure diagnosis, simulator/device z-to-z broadcast capture using the sanitized bad-txn diagnostics, Core/vector comparison if spend/output/binding `bad-txns-*` persists after valid witness fetch, deployed capable/non-capable PIVX v1 node manual tests, physical-device height-aware shielded-sync start/checkpoint/final logs, node-list screenshots showing `Sapling v1 ready` versus legacy/unavailable/fail-closed states, generated shielded receive-address restart/selectability evidence, malformed/null transparent balance response evidence, transparent/shielded mempool acceptance tests around the Core dust thresholds, activation-source confirmation, manual shielded transaction detail/restart/reorg checks, physical-device PIVX dashboard evidence for the shielded balance card without Litecoin MWEB actions, mixed transparent/shielded send-screen balance/send-all/route evidence, release-owner proving-parameter host/mirror approval, Tor/interruption/low-storage/iOS background and physical-device proving-parameter evidence, release CI native artifact provenance plus Android/iOS physical-device native self-tests, clean-device encrypted-sidecar backup/restore evidence, multi-gap transparent restore tests, diversified shielded address-reuse restore tests, Android/iOS secure-screen screenshot/app-switcher evidence, and product-approved seed-only/viewing-key recovery guidance.
- Use `14_electrumx_followups.md` and `15_electrumx_update_from_other_agent.md` for the PIVX ElectrumX v1 server context. The wallet-facing docs now match the reported v1 shape, but do not treat the server update or docs alignment as wallet readiness until tested against Cake Wallet and default nodes.
- Use `09_release_gate_checklist.md` as the release owner checklist.
- Use `10_test_plan.md` as the manual QA script after fixes land.
- Keep production-code changes separate from audit-doc updates unless explicitly requested.

## Recommended Next Prompt (2026-06-12 update)

Highest-value next steps, in order: (1) run the iPhone 16e simulator z-to-z send against the now-green default nodes and capture `Witness source summary: anchor_bound` plus successful post-broadcast shielded sync — this closes the active send gate; (2) rebuild iOS/Android native artifacts from current Rust source (`bash cw_pivx/scripts/build_ios.sh`, `bash cw_pivx/scripts/build_android.sh`) since the Rust FFI gained the z-to-t route; (3) simulator z-to-t (deshield) send to an own transparent address with mempool/confirmation evidence; (4) implement t-to-z (shield) using `TransparentUtxoData` keys, secp256k1 signing, and the per-input sighash legs from PIVX Core `interpreter.cpp` `SignatureHash` (prevout + scriptCode + amount + nSequence before locktime/hashtype; local Core checkout at `/Users/liquid/Projects/PIVX`); (5) add z-to-t route labels to shielded history/transaction details. The original recommended prompt below predates these updates.

## Recommended Next Prompt

Continue closing PIVX release blockers from `09_release_gate_checklist.md`. Use the repo-pinned FVM toolchain. Do not start external APK/TestFlight preparation. The 2026-06-05 iPhone 16e iOS 26.1 simulator z-to-z send got past proving and broadcast: anchor-bound `get_witness(commitment, anchor)` failed with sanitized `witness_lookup_failed`, commitment-only witness fallback succeeded, FFI proving returned, and broadcast returned a txid string with sanitized shape `bytes=2365 version=3 type=0 vin=0 vout=0 sapling=present value_balance=2365000 shielded_spends=1 shielded_outputs=2 binding_sig=present parse=ok`; no `bad-txns-*` rejection appeared. The active ElectrumX blocker is anchor-bound real-note `blockchain.sapling.get_witness(commitment, anchor)` correctness plus post-broadcast shielded sync, which failed and left the dashboard showing `Shielded sync failed` in `/private/tmp/pivx_z_to_z_after_broadcast.png`. A 2026-06-05T11:22:31Z bundled default-node probe after reported node updates found all four Chainster endpoints green, including v1 metadata, live helpers, sampled ranges, and current-tip/previous-tip single-height envelopes; the prior 2026-06-05T11:08:52Z `electrum02` raw timeout symptom did not reproduce. Treat future raw timeouts through the `14_electrumx_followups.md` timeout/degraded-state prompt, but keep the current send blocker focused on anchor-bound witnesses and post-broadcast shielded sync.

The wallet now logs sanitized witness failure reason codes, carries sanitized accepted witness source metadata (`anchor_bound` versus `commitment_only_fallback`), logs sanitized post-broadcast shield-sync errors, and subscribes PIVX shielded sync to block headers with a throttle of `PivxNetwork.targetBlockTime` (`60` seconds), matching PIVX block time. PIVX transparent sync remains Electrum subscription-driven, not a fixed full-wallet polling timer. PIVX QR/URI work now routes selected shielded receive address/URI copies through the sensitive clipboard helper on supported mobile platforms, flags PIVX URI `memo`/`req-*` fields as unsupported, and warns/ignores those fields in send scan/paste instead of populating Sapling memos. Seed, Wallet Keys, and PIVX receive routes now force a scoped native secure-screen mode without changing the persisted global screenshot setting. Focused verification passed: `fvm dart analyze cw_pivx/lib/src/sapling/pivx_sapling_electrumx.dart cw_pivx/lib/src/sapling/sapling_factories.dart cw_pivx/test/pivx_sapling_electrumx_test.dart` exits 0; `fvm flutter test cw_pivx/test/pivx_sapling_electrumx_test.dart --no-pub` passes all 26 focused ElectrumX tests; `fvm dart analyze cw_pivx/lib/src/pivx_wallet.dart cw_pivx/test/cw_pivx_test.dart` reports only pre-existing `pivx_wallet.dart` deprecation infos; `fvm flutter test cw_pivx/test/cw_pivx_test.dart --no-pub` passes all 18 focused PIVX tests; `fvm dart analyze lib/view_model/wallet_address_list/wallet_address_list_view_model.dart lib/src/screens/receive/widgets/qr_widget.dart lib/src/screens/receive/receive_page.dart lib/utils/payment_request.dart lib/src/screens/send/widgets/send_card.dart test/utils/payment_request_test.dart` exits 0; `fvm flutter test test/utils/payment_request_test.dart --no-pub` passes all 9 focused payment URI tests; `fvm dart analyze lib/utils/secure_screen.dart lib/src/screens/base_page.dart lib/src/screens/seed/wallet_seed_page.dart lib/src/screens/wallet_keys/wallet_keys_page.dart lib/src/screens/receive/receive_page.dart test/utils/secure_screen_test.dart` exits 0 with only pre-existing `wallet_keys_page.dart` deprecation infos; and `fvm flutter test test/utils/secure_screen_test.dart --no-pub` passes all 4 focused secure-scope tests. Next simulator/device z-to-z acceptance should show `Witness source summary: anchor_bound`, not `commitment_only_fallback`.

Continue the bad-txn lane only if a future simulator/device broadcast returns `bad-txns-*`: capture the sanitized summary plus exact Core rejection string and move to `PIVX-SEND-007` Core/vector comparison. Use `14_electrumx_followups.md` for the updated ElectrumX agent prompt: fix `get_witness(commitment, anchor)` so the wallet does not need commitment-only fallback, and verify post-broadcast single-height block ranges return complete v1 envelopes or structured retryable errors.

Keep every verification gate open until independent PIVX Core/ElectrumX/proving-parameter release-owner evidence, physical-device height-aware shielded sync/send evidence, release CI native provenance, Tor/interruption/low-storage/iOS-background evidence, Android/iOS physical-device native self-tests, node-list screenshots, malformed/null transparent balance UX evidence, Core dust-threshold mempool acceptance, clean-device encrypted-sidecar backup/restore, restore/address-reuse, shielded transaction detail/restart/reorg device checks, Android/iOS QR/copy/share clipboard evidence, Android/iOS screenshot/app-switcher secure-screen evidence, and product-approved seed-only/viewing-key/URI-memo guidance all pass. Update `09_release_gate_checklist.md`, `13_next_session_context.md`, and `14_electrumx_followups.md` as work finishes.

## Important Files Discovered

- `cw_pivx/lib/src/pivx_wallet.dart`
- `cw_pivx/lib/src/pivx_wallet_service.dart`
- `cw_pivx/lib/src/pivx_wallet_addresses.dart`
- `cw_pivx/lib/src/pivx_wallet_creation_credentials.dart`
- `cw_pivx/lib/src/pivx_network.dart`
- `cw_pivx/lib/src/pivx_transaction_priority.dart`
- `cw_pivx/lib/src/pending_pivx_shielded_transaction.dart`
- `cw_pivx/lib/src/sapling/pivx_sapling_electrumx.dart`
- `cw_pivx/lib/src/sapling/sapling_constants.dart`
- `cw_pivx/lib/src/sapling/sapling_factories.dart`
- `cw_pivx/lib/src/sapling/sapling_ffi.dart`
- `cw_pivx/lib/src/sapling/sapling_note_storage.dart`
- `cw_pivx/lib/src/sapling/sapling_transaction_builder.dart`
- `cw_pivx/lib/src/sapling/native_sapling_key_manager.dart`
- `cw_pivx/lib/src/sapling/native_sapling_transaction_builder.dart`
- `cw_pivx/lib/src/sapling/native_shield_sync_engine.dart`
- `cw_pivx/rust/src/ffi.rs`
- `cw_pivx/rust/src/keys.rs`
- `cw_pivx/rust/src/prover.rs`
- `cw_pivx/rust/src/sync.rs`
- `cw_pivx/rust/src/notes.rs`
- `cw_pivx/rust/src/transaction.rs`
- `lib/pivx/pivx.dart`
- `lib/pivx/cw_pivx.dart`
- `lib/view_model/wallet_creation_vm.dart`
- `lib/view_model/wallet_new_vm.dart`
- `lib/view_model/wallet_restore_view_model.dart`
- `lib/src/screens/new_wallet/advanced_privacy_settings_page.dart`
- `lib/src/screens/restore/wallet_restore_from_seed_form.dart`
- `lib/src/widgets/blockchain_height_widget.dart`
- `lib/view_model/wallet_address_list/wallet_address_list_view_model.dart`
- `lib/view_model/wallet_address_list/wallet_address_edit_or_create_view_model.dart`
- `lib/src/screens/receive/receive_page.dart`
- `lib/src/screens/receive/widgets/qr_widget.dart`
- `lib/src/screens/receive/widgets/address_list.dart`
- `lib/src/screens/receive/widgets/address_cell.dart`
- `lib/view_model/send/send_view_model.dart`
- `lib/view_model/send/output.dart`
- `lib/src/screens/send/widgets/send_card.dart`
- `lib/core/address_validator.dart`
- `lib/core/payment_uris.dart`
- `lib/utils/payment_request.dart`
- `lib/view_model/dashboard/balance_view_model.dart`
- `lib/src/screens/dashboard/pages/balance/balance_row_widget.dart`
- `lib/src/screens/dashboard/pages/balance/crypto_balance_widget.dart`
- `lib/view_model/dashboard/transaction_list_item.dart`
- `lib/src/screens/dashboard/pages/transactions_page.dart`
- `lib/view_model/transaction_details_view_model.dart`
- `lib/src/screens/dashboard/widgets/sync_indicator.dart`
- `lib/core/sync_status_title.dart`
- `lib/src/screens/settings/connection_sync_page.dart`
- `lib/src/widgets/standard_list_status_row.dart`
- `lib/view_model/node_list/node_create_or_edit_view_model.dart`
- `lib/src/screens/nodes/widgets/node_form.dart`
- `lib/src/screens/settings/security_backup_page.dart`
- `lib/view_model/wallet_keys_view_model.dart`
- `assets/pivx_electrum_server_list.yml`
- `lib/entities/default_settings_migration.dart`
- `lib/entities/node_list.dart`
- `lib/core/node_switching_service.dart`
- `lib/store/settings_store.dart`
- `lib/core/backup_service.dart`
- `lib/core/backup_service_v3.dart`
- `cw_core/lib/node.dart`
- `cw_core/lib/unspent_coin_type.dart`
