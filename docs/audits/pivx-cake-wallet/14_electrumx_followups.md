# PIVX ElectrumX Follow-Ups

Status: Open.

Date: 2026-05-27

Last updated: 2026-06-11

Scope: server-side work needed in the PIVX Sapling ElectrumX repository. Keep these tasks separate from Cake Wallet mobile-client changes. Do not use this document as APK/TestFlight readiness evidence until the matching wallet gates and manual tests pass.

## Wallet-Side Assumptions From Current Pass

- Update from `15_electrumx_update_from_other_agent.md`: another agent reports that the server-side v1 support has been implemented under contract id `pivx.sapling.electrumx.v1`, with primary capability method `blockchain.sapling.capabilities`, v1 block-range envelopes, global output positions, anchor-bound witnesses, block hashes, rollback behavior, and 62 passing server tests. Cake Wallet now accepts `blockchain.sapling.capabilities` and stores/display capability metadata, but this document remains Open until those server claims are verified against the wallet and default nodes.
- Cake Wallet's client-facing Sapling docs were aligned on 2026-06-01 to that reported v1 contract. Legacy method aliases are compatibility-only and are not enough to close default-node readiness without the v1 capability/envelope/global-position/anchor-bound witness behavior.
- Cake Wallet must treat Sapling range RPC failures as hard sync failures. Failed ranges must not be converted to empty ranges, must not advance `lastSyncedHeight`, and must be retried or surfaced to the user.
- Cake Wallet now persists a `nextTreePosition` cursor locally, supports explicit output positions when returned by `get_block_range`, and refuses to start post-activation/birthday Sapling sync without either a trusted persisted cursor or server-advertised global output positions. Legacy inferred cursors from owned notes are not trusted.
- Cake Wallet now supports a proposed v1 `get_block_range` envelope with `complete: true` and rejects incomplete envelopes or mismatched range metadata.
- Cake Wallet probes `blockchain.sapling.capabilities` first, then `blockchain.sapling.get_capabilities` when available, and validates advertised network and Sapling activation height. Legacy servers are treated as block-range-only unless they implement the v1 capability method.
- Cake Wallet automatic node switching now requires the explicit `pivx.sapling.electrumx.v1` release contract predicate before switching to a PIVX candidate node.
- Cake Wallet manual PIVX node testing now fails a reachable Electrum node unless it advertises the v1 release contract with block ranges, canonical global positions, best anchor, anchor-bound witnesses, block hashes, structured errors, and the required v1 methods. PIVX node records now persist capability/version metadata and the node list distinguishes `Sapling v1 ready` from `Sapling legacy only` after probing.
- Cake Wallet dashboard and Connection/Sync status now show shielded sync progress, last shielded scanned height, Sapling RPC readiness, and sanitized shielded sync errors. Durable per-node capability/version badges still require the server to return stable v1 capability metadata.
- Cake Wallet now emits sanitized height-only PIVX Sapling sync evidence for sync start, first completed range, coarse checkpoints, and final completed range. Use these logs in default-node manual tests to prove whether shielded scanning started at activation, explicit restore height, or fresh-wallet birthday fallback without exposing shielded metadata.
- Cake Wallet blocks unsafe unsupported routes locally, and shielded sends now require node anchor/witness capability before transaction construction. The server contract still needs to support route validation, witness/anchor retrieval, and nullifier status for enabled routes before external testing.
- Cake Wallet z-to-z send-max, fee-aware note selection, and shielded history rows now have wallet-side mitigations, but server-side v1 witness/anchor/nullifier/history evidence remains required before any external APK/TestFlight.
- Mainnet is the first external-test policy unless product direction changes. Testnet support must remain wired. The ElectrumX agent reports PIVX Core v5.6.1 activation heights of mainnet `2700500` and testnet `201`, and the wallet constants now use those values; keep the release gate open until the release owner records independent Core-source evidence and device tests.

## Current Server-Side Release Blockers

These items are still open as of 2026-06-01 and block closing the matching mobile release gates:

| Server-side area | Blocks mobile findings | Required proof |
| --- | --- | --- |
| Capability/version probe | `PIVX-NET-002`, `PIVX-NET-003` | `blockchain.sapling.capabilities` (or release-equivalent alias) returns network, activation height, supported methods, response limits, and version metadata. |
| Complete range envelope | `PIVX-REC-002` | Empty successful ranges are distinguishable from daemon/index/method failures, and failures produce structured errors. |
| Canonical global output positions | `PIVX-REC-001`, `PIVX-SEND-001` | Every returned Sapling output includes its global commitment tree position in PIVX Core canonical order. |
| Anchor/best-anchor and anchor-bound witnesses | `PIVX-SEND-001`, `PIVX-SEND-006` | `blockchain.sapling.get_best_anchor` returns a usable anchor/root plus height, and witness responses include anchor/root and height that can be verified against the selected transaction anchor. |
| Reorg/hash data | `PIVX-REC-005`, `PIVX-BAL-009` | Returned blocks include hashes and enough rollback behavior to support at least the chosen reorg depth. |
| Canonical network constants | `PIVX-NET-001`, `PIVX-NET-002` | Mainnet/testnet activation heights are confirmed from the selected PIVX Core release commit and match wallet constants. |

Until these are implemented and manually verified against the wallet, mobile-side `Needs Verification` findings tied to Sapling sync/send correctness must stay open.

Server update to verify, 2026-05-28: `15_electrumx_update_from_other_agent.md` reports the capability probe as `blockchain.sapling.capabilities`, not `blockchain.sapling.get_capabilities`, and reports all rows in this table implemented in the ElectrumX fork. Treat that as incoming evidence, not closure, until Cake Wallet is tested against that server and default-node deployments expose the same v1 metadata.

Default-node probe, 2026-05-31: the currently shipped Chainster nodes in `assets/pivx_electrum_server_list.yml` are reachable (`electrum02.chainster.org` reports `ElectrumX 1.19.0`; `electrum01.chainster.org` reports `ElectrumX 1.16.0`). `electrum02` has partial legacy Sapling RPC support: `blockchain.sapling.get_block_range` returns a bare list with block hash/time and Sapling tx output fields, and `blockchain.sapling.get_witness` is registered enough to return `commitment not found` for a dummy commitment. It does not yet expose `blockchain.sapling.capabilities`, `blockchain.sapling.get_capabilities`, v1 envelopes, global output positions, best-anchor, nullifier-status, or commitment-info methods. `electrum01` still returns `unknown method` for sampled Sapling methods. This means the full server-side v1 implementation reported in `15_electrumx_update_from_other_agent.md` is not yet deployed on the bundled defaults, or the bundled defaults are not fully upgraded v1 nodes. Keep this document open until default-node deployments expose the v1 metadata and Cake Wallet is tested against them.

Client documentation alignment, 2026-06-01: `cw_pivx/SAPLING.md` and the `PIVXSaplingElectrumX` header now document the reported `pivx.sapling.electrumx.v1` contract, including `blockchain.sapling.capabilities`, `get_block_range` v1 envelopes, `block_hashes`, canonical global output positions, anchor-bound witness metadata, and structured failure semantics. This is a documentation/client-alignment update only; it is not release evidence until the wallet is exercised against deployed v1/default nodes.

Wallet-side v1 readiness update, 2026-06-02: Cake Wallet now classifies a PIVX node as release-ready only when `SaplingRpcCapabilities.supportsV1ReleaseContract` passes for the reported `pivx.sapling.electrumx.v1` contract. Legacy block-range fallback remains compatibility-only and is not enough for node-list readiness, manual node testing, or automatic switching. Focused local wallet tests cover complete v1 metadata, incomplete advertised v1 metadata, legacy fallback, and null malformed capability responses, but this remains wallet-side evidence only until deployed v1/default nodes are tested.

Wallet-side sync evidence update, 2026-06-04: Cake Wallet's ElectrumX range callback now provides range start and end to the sync wrapper, and the wallet logs sanitized start/first-range/checkpoint/final-range PIVX Sapling sync heights. Focused local tests cover failed-range non-completion and checkpoint-selection behavior. This removes a client observability gap for the next default-node run, but it does not close server readiness until `electrum01` and `electrum02` both pass deployed v1 capability/range probes and Cake Wallet shielded sync completion is captured on simulator/device.

Repeatable default-node probe update, 2026-06-04: Cake Wallet now has `tool/pivx_sapling_default_node_probe.dart` for wallet-secret-free default-node v1 capability/range evidence. `fvm dart run tool/pivx_sapling_default_node_probe.dart electrum01.chainster.org:50002:ssl electrum01.chainster.org:50001:plain` showed both `electrum01` endpoints reachable with `ElectrumX 1.19.0`, `pivx.sapling.electrumx.v1`, `release_contract_ready: true`, mainnet activation `2700500`, required v1 methods, global positions, block hashes, and complete sampled range envelopes with `block_hash_count=100`. `electrum02` refused connections during the same pass and is release-owner/user-reported reindexing. Keep this document open until `electrum02` finishes reindexing and passes the same probe, then verify Cake Wallet shielded sync completion against the default nodes.

Live helper-method blocker, 2026-06-04: the same probe was expanded to call safe helper methods after capability/range checks. `electrum01.chainster.org:50002` and `:50001` both still advertise complete v1 metadata and complete sampled range envelopes, but `blockchain.sapling.get_best_anchor` returns RPC `-32603` internal server error on both endpoints. A dummy all-zero `blockchain.sapling.get_nullifier_status` call returns `spent=false`; the dummy commitment-info call currently returns `null`, which also needs explicit contract confirmation because Cake Wallet's v1 client expects a structured map shape. Cake Wallet now fails closed during live v1 release-method validation when an advertised v1 node cannot satisfy best-anchor, nullifier-status, and commitment-info parsing. Keep default-node readiness, node-list readiness, auto-switching, and shielded-send gates open until `electrum01` passes these live helper probes and `electrum02` completes reindexing and passes the same checks.

Same-day retest, 2026-06-04T01:23:31Z: running `fvm dart run tool/pivx_sapling_default_node_probe.dart` against the bundled default list still found `electrum02.chainster.org:50002` and `:50001` refusing connections. `electrum01` SSL/plain remained reachable with v1 metadata and complete sampled ranges, but `get_best_anchor` still returned RPC `-32603` internal server error, dummy nullifier remained `spent=false`, dummy commitment-info remained `null`, and `live_helper_methods_ready=false`.

Targeted electrum01 retest, 2026-06-04T01:28:32Z: `fvm dart run tool/pivx_sapling_default_node_probe.dart electrum01.chainster.org:50002:ssl electrum01.chainster.org:50001:plain` remained unchanged. Both endpoints were reachable with complete v1 metadata and complete sampled ranges, but `get_best_anchor` still returned RPC `-32603` internal server error, dummy nullifier remained `spent=false`, dummy commitment-info remained `null`, and `live_helper_methods_ready=false`.

Bundled default-node retest after server updates, 2026-06-04T11:11:29Z: `fvm dart run tool/pivx_sapling_default_node_probe.dart` found all four shipped Chainster endpoints reachable (`electrum01`/`electrum02`, SSL/plain) with `ElectrumX 1.19.0`, `blockchain.sapling.capabilities`, contract `pivx.sapling.electrumx.v1`, `release_contract_ready: true`, mainnet activation `2700500`, global positions, block hashes, required v1 methods, complete sampled range envelopes, successful `get_best_anchor` responses, dummy nullifier `spent=false`, dummy commitment-info structured `exists=false`, and `live_helper_methods_ready=true`. This closes the previous safe-helper probe symptom only; it is not shielded-send readiness.

Live Cake Wallet simulator retest after the same server update, 2026-06-04T11:17Z: the iPhone 16e simulator app restored encrypted spendable notes, reached generic `Synced`, loaded proving parameters, selected a shielded note, and fetched a spend anchor. Shielded send then failed while fetching the selected-note witness: `commitment_anchor` failed, `commitment_only` failed, and the user-facing error remained `canonical_witness_unavailable` / witness backend not implemented correctly. Treat this as the active ElectrumX blocker: the backend can advertise v1 helpers and return anchors, but it still cannot return canonical Sapling witness paths for real wallet commitments. The same run also logged a tip single-height sync failure: `get_block_range` for `5441053-5441053` returned an incomplete range after retries. Keep shielded-send, default-node readiness, node-list readiness, and release gates open until real wallet witness paths are canonical and tip-range completeness is fixed.

Wallet-side bad-txn diagnostic update, 2026-06-05: Cake Wallet now records a sanitized PIVX Sapling transaction-shape summary on shielded broadcast attempts and maps exact PIVX Core `bad-txns-*` Sapling rejection strings into spend-proof/signature, output-proof, binding-signature, anchor/nullifier, value-balance, nullifier, activation, or form lanes. This does not reduce the active ElectrumX requirement: `get_witness` must first return canonical real-note witnesses so the simulator can reach broadcast. If the next simulator run proceeds past witness fetch and then Core rejects the transaction, hand off the sanitized summary plus exact Core rejection string to the wallet/Core `PIVX-SEND-007` lane instead of treating it as another capability-probe failure.

Live Cake Wallet simulator retest, 2026-06-05: after wallet-side witness-attempt ordering was corrected, the iPhone 16e iOS 26.1 simulator z-to-z send loaded proving parameters, selected a shielded note, fetched a spend anchor, tried anchor-bound `get_witness(commitment, anchor)` first, and classified that attempt as `witness_lookup_failed`. The compatibility `get_witness(commitment)` fallback then returned a witness accepted by Cake Wallet with 32 canonical-looking 64-char path nodes, the wallet used the witness-returned spend anchor, FFI proving returned, and broadcast returned a string txid. The sanitized broadcast summary was `bytes=2365 version=3 type=0 vin=0 vout=0 sapling=present value_balance=2365000 shielded_spends=1 shielded_outputs=2 binding_sig=present parse=ok`; no `bad-txns-*` rejection appeared in the captured log. Post-broadcast shielded sync then failed and left the dashboard showing `Shielded sync failed`; the wallet now logs sanitized post-broadcast sync failure reasons. This retest moves the immediate ElectrumX witness ask from "all real-note witness lookup is unavailable" to "anchor-bound witness lookup is still not accepted while commitment-only fallback works." Keep the gate open until anchor-bound witness works and post-broadcast shielded sync succeeds without leaving the dashboard stale.

Default-node timeout retest, 2026-06-05T11:08:52Z: `fvm dart run tool/pivx_sapling_default_node_probe.dart` now includes current-tip and previous-tip single-height `get_block_range` checks. `electrum01.chainster.org:50002` and `:50001` both remain reachable with v1 metadata, `live_helper_methods_ready=true`, complete sampled 100-block ranges, and complete empty single-height tip ranges. `electrum02.chainster.org:50002` was reachable and advertised the v1 contract, but dummy `get_commitment_info` timed out, all three sampled `get_block_range` calls timed out, and current-tip lookup was unavailable. `electrum02.chainster.org:50001` timed out at `server.version`. Treat this as a separate deployed-node reliability/index/load blocker from the anchor-bound witness blocker: green capability metadata is not enough if helper/range calls time out under the default probe.

Default-node timeout follow-up, 2026-06-05T11:16:50Z: Cake Wallet's default-node probe now emits endpoint-level `endpoint_probe_ready` and `degraded_reasons` fields after capability, live helper, sampled range, current-tip, and previous-tip checks. A fresh bundled-default run found all four Chainster endpoints reachable with `ElectrumX 1.19.0`, `pivx.sapling.electrumx.v1`, `release_contract_ready=true`, `live_helper_methods_ready=true`, complete sampled envelopes for `2700500-2700599`, `2700900-2700999`, and `2705000-2705099`, complete current-tip and previous-tip single-height envelopes, `endpoint_probe_ready=true`, and `degraded_reasons=none`. This clears the latest raw-timeout symptom only. Keep the timeout prompt below as the deployment/runbook response if raw timeouts recur; the active server blocker remains anchor-bound real-note `get_witness(commitment, anchor)` plus post-broadcast shielded sync.

Node-update confirmation retest, 2026-06-05T11:22:31Z: after reported node updates, `fvm dart run tool/pivx_sapling_default_node_probe.dart` again found all four bundled Chainster endpoints reachable and healthy. Each endpoint returned v1 capability metadata, `live_helper_methods_ready=true`, complete sampled activation-era and empty later range envelopes, complete current-tip and previous-tip single-height envelopes, `endpoint_probe_ready=true`, and `degraded_reasons=none`. Treat the raw-timeout blocker as not reproduced in this pass. The remaining ElectrumX acceptance item is still the real-wallet path: Cake Wallet must accept `get_witness(commitment, anchor)` directly for the selected note and post-broadcast shielded sync must complete.

Wallet-side witness-source diagnostic update, 2026-06-11: Cake Wallet now carries a sanitized witness `source` through accepted witness results and shielded transaction construction. The next simulator/device z-to-z capture should include `Witness source summary: anchor_bound` when the release-contract path succeeds, or `commitment_only_fallback` when Cake Wallet still had to fall back to `get_witness(commitment)`. This is not server readiness evidence by itself; it exists to make the active ElectrumX acceptance criterion explicit in wallet logs while keeping the compatibility fallback available.

Wallet-side recovery-guidance UI update, 2026-06-11: Cake Wallet added PIVX seed-only recovery guidance in the Wallet Keys seed tab. This does not change the ElectrumX blocker or acceptance criteria; the server-side release ask remains anchor-bound real-note `get_witness(commitment, anchor)` plus post-broadcast shielded sync completion.

Wallet-side spend-maturity diagnostic update, 2026-06-11: Cake Wallet now logs sanitized count-only z-to-z spend eligibility diagnostics for the provisional confirmation policy. This is local wallet observability only; ElectrumX still needs to return canonical anchor-bound witnesses and complete post-broadcast range envelopes.

Wallet-side QR/URI/clipboard policy update, 2026-06-11: Cake Wallet now routes selected PIVX shielded receive address/URI copies through its sensitive clipboard helper on supported mobile platforms, flags PIVX URI `memo`/`req-*` fields as unsupported, and warns/ignores those fields in send scan/paste instead of populating Sapling memos. This is local receive/send UI policy only and does not change the ElectrumX acceptance criteria: anchor-bound real-note `get_witness(commitment, anchor)` and complete post-broadcast block-range envelopes still must pass.

Wallet-side secure-screen policy update, 2026-06-11: Cake Wallet now has a route-lifetime scoped native secure-screen helper that does not change the persisted global screenshot setting. Wallet seed, Wallet Keys, and PIVX receive routes force that scope. This is local mobile-security/UI policy only and does not change the ElectrumX acceptance criteria.

Wallet-side range-error classification update, 2026-06-11: Cake Wallet now sanitizes incomplete `get_block_range` responses and structured retryable server/index states (`partial_index`, `index_not_ready`, `backend_timeout`) to a distinct "complete Sapling block range not returned yet" message instead of treating them like unsupported block scanning. This is local observability only. ElectrumX still must return complete v1 envelopes for post-broadcast single-height ranges, or a structured retryable error that does not look complete and can be retried by the wallet/operator.

Wallet-side route-warning update, 2026-06-11: Cake Wallet now classifies the PIVX send route matrix in the send view model and shows an inline warning for unsupported `t-to-z`, `z-to-t`, mixed-output, and ambiguous-source sends before transaction construction. This is local route UX only and does not change the ElectrumX acceptance criteria: real-note `get_witness(commitment, anchor)` must work without commitment-only fallback, and post-broadcast shielded sync must complete or return structured retryable range errors.

Wallet-side FFI seed-copy zeroization update, 2026-06-11: Cake Wallet now overwrites the native `malloc<Uint8>` BIP39 seed copy before freeing it after PIVX Sapling key initialization. This is local mobile-security hardening only and does not change the ElectrumX acceptance criteria.

Wallet-side FFI returned-allocation zeroization update, 2026-06-11: Cake Wallet's Rust FFI free paths now wipe library-owned returned C strings and binary buffers before free, including both `cw_pivx_*` and legacy `pivx_*` free APIs, and `cw_pivx_free_buffer()` releases binary buffers with `libc::free()` to match their `libc::malloc()` allocation path. This is local mobile-security hardening only and does not change the ElectrumX acceptance criteria.

Wallet-side Dart FFI input zeroization update, 2026-06-11: Cake Wallet now wipes Dart-owned native UTF-8 inputs and short-lived Sapling byte buffers before `malloc.free()` across PIVX Sapling FFI calls, including note JSON, transaction notes JSON, optional memos, anchors, proving-parameter paths, trial-decryption buffers, and nullifier checks. This is local mobile-security hardening only and does not change the ElectrumX acceptance criteria.

Wallet-side Rust transaction-result staging zeroization update, 2026-06-11: Cake Wallet's Rust shielded-transaction result path now zeroizes staging JSON strings and temporary txid/raw-transaction hex/error strings after copying them into the returned FFI buffer. This is local mobile-security hardening only and does not change the ElectrumX acceptance criteria.

Wallet-side Rust spendable-note input zeroization update, 2026-06-11: Cake Wallet's Rust `SpendableNoteData` now zeroizes sensitive deserialized note-input strings on drop, including rseed, witness, nullifier, optional commitment, and optional memo material. This is local mobile-security hardening only and does not change the ElectrumX acceptance criteria.

Wallet-side Rust transfer-struct zeroization update, 2026-06-11: Cake Wallet's Rust transaction result, transparent UTXO, and transaction option transfer structs now zeroize tx hex/nullifier, private-key, memo, and address strings on drop. This is local mobile-security hardening only, does not enable external shield/deshield routes, and does not change the ElectrumX acceptance criteria.

Real-commitment witness probe evidence, 2026-06-12: `tool/pivx_sapling_default_node_probe.dart` now extracts a real on-chain Sapling commitment (public chain data, no wallet secrets) from the activation-era ranges and validates anchor-bound plus commitment-only `get_witness` against it with the same acceptance rules as the wallet client. Bundled-default results: both `electrum01` endpoints returned `real_witness_anchor_bound: success source=anchor_bound` with 32 canonical path nodes, matching anchor root/height, and matching commitment/position — the first direct evidence that the deployed server can satisfy the wallet's anchor-bound release criterion without commitment-only fallback. Both `electrum02` endpoints failed anchor-bound witness with a 30s client timeout while commitment-only succeeded, and `electrum02` `get_best_anchor` served a fallback anchor (`anchor_height` 146+ blocks behind tip, no `root` field) that is neither the canonical root nor its byte-reverse. The probe also flags structured `available=false`/`success=false` helper responses as `unavailable` instead of success.

Server-side root cause and fix, 2026-06-12 (implemented in the sibling `electrum` repo working tree, not yet deployed): the live Sapling tree has 44,934 outputs, so a full canonical root costs ~90k Pedersen hashes (~10s on `electrum02` hardware). `_sapling_current_anchor_from_helper` exceeded the 8s helper timeout on `electrum02`, so `get_best_anchor` silently fell back to a legacy daemon/tree-state anchor whose node encoding can never match canonical helper witness roots; any witness bound to that anchor sent the helper into an O(boundaries x full-root) historical replay that timed out. Fixes implemented: (1) the witness helper now uses an append-only frontier for historical-anchor search (O(leaves + boundaries x depth)), rayon-parallel level reduction, and a precomputed empty-node table — at live tree scale a current-anchor witness drops to ~0.3s and worst-case anchor-not-found is bounded at ~6s; (2) anchor heights are now derived from the last leaf of the selected tree, not the chain tip, so best-anchor and witness responses stay consistent across Sapling-quiet blocks; (3) the canonical anchor and commitment caches are keyed on tree contents instead of chain height so they survive new blocks without Sapling outputs; (4) `get_best_anchor` now fails closed with a structured `available: false` + `canonical_anchor_unavailable` error instead of serving legacy daemon/tree-state anchors; (5) `tests/server/test_env.py` no longer leaks a cleared `os.environ` into later test files, which had broken every helper-building Sapling test in full-suite runs. All 38 PIVX Sapling server tests pass, including new coverage for anchor stability across Sapling-quiet blocks and historical-anchor witnesses; the full server suite is strictly better than its pre-change baseline. Deployment ask: deploy the updated server plus rebuilt release helper binary to both Chainster nodes, then rerun the bundled-default probe expecting `real_witness_anchor_bound: success` on all four endpoints, and treat any `get_best_anchor` `available: false` as a fail-closed signal rather than a fallback anchor.

Live deployment status and witness-latency fix, 2026-06-12 (second pass): after the release owner rolled the first server update, the real-commitment probe confirmed `electrum01` (both ports) now serves anchor-bound real witnesses with the new stable last-leaf `anchor_height` and matching best-anchor metadata. `electrum02` came back reachable but still served the legacy fallback anchor (`available=true`, no `root` field) and timed out anchor-bound witness — it is still running the previous `session.py`. The release owner also reported `get_witness elapsed=2.537s` slow-log lines on the deployed VPS. In response the witness helper gained an on-disk Merkle level cache (`PIVX_SAPLING_WITNESS_STATE_FILE`, defaulted by the server into the ElectrumX DB directory as `pivx_sapling_witness_state.bin`): repeat invocations verify the cached prefix with a rolling SHA-256 chain, append only new leaves, and answer current-anchor witness/root calls with sibling lookups. Measured at the live 44,934-output scale: cold build ~184ms, warm witness ~73ms, warm best-anchor root ~62ms, incremental append ~83ms, with cached results bit-identical to stateless rebuilds and verified by the helper's verify mode. Historical-anchor lookups still use the bounded frontier replay (~6s worst case). All 39 server Sapling tests pass, including a session-level state-cache identity test. Deployment ask: redeploy BOTH nodes with the latest `session.py` plus a rebuilt release helper binary, confirm `pivx_sapling_witness_state.bin` appears in the DB directory, then rerun the bundled probe expecting `real_witness_anchor_bound: success` on all four endpoints and sub-second `get_witness` log timings.

Deployment confirmation, 2026-06-12 (third pass): after the release owner redeployed both nodes with the anchor/witness fixes and the tree-state cache, the real-commitment probe found ALL FOUR bundled Chainster endpoints green: `real_witness_anchor_bound: success source=anchor_bound` with 32 canonical nodes, canonical best anchors with `root` and `tree_size`, matching anchor heights (including correct tracking when the tree grew from 44,934 to 44,936 outputs mid-pass), and complete tip single-height range envelopes. The active server-side anchor-bound witness blocker is now closed with live default-node evidence. Remaining acceptance is wallet-side only: a simulator/device z-to-z send must log `Witness source summary: anchor_bound` and complete post-broadcast shielded sync.

## Required ElectrumX Work

- Define a versioned PIVX Sapling RPC v1 capability probe, currently reported as `blockchain.sapling.capabilities`, returning server version, PIVX Core version, network, Sapling activation height, supported methods, max range size, response ordering guarantee, block-hash support, structured-error support, and whether explicit global output positions are included.
- Extend `blockchain.sapling.get_block_range` so each successful response is unambiguously complete. The reported v1 envelope includes success/completion/empty state, requested start/end range metadata, height/block/Sapling transaction counts, `block_hashes`, blocks, and structured errors for failed or partial ranges. Empty success must be distinguishable from method failure, and returned range metadata must exactly match the requested range.
- Add explicit global Sapling output positions for every returned output. The server should index and return positions in canonical PIVX Core block transaction order and Sapling output order.
- Include block hash and timestamp for every returned block, and define behavior for reorgs and orphaned hashes.
- Implement or alias the final method names used by Cake Wallet: nullifier status, commitment info, block range, best anchor, anchor height, tree state, and witness.
- Make witness responses anchor-bound. Include anchor/root, anchor height, position, path, and commitment; clients must be able to verify the witness reconstructs exactly the requested anchor.
- Return canonical Sapling commitment-tree witness nodes, not DB blobs, txids/block hashes, arbitrary hashes, reversed non-field bytes, or non-canonical field elements. Cake Wallet/librustpivx must be able to parse every 32-byte sibling with the same encoding expected by `zcash_primitives::sapling::Node::read`.
- Fix the deployed anchor-bound witness shape specifically. As of 2026-06-05, Cake Wallet can proceed with commitment-only fallback, but `get_witness(commitment, anchor)` still fails for a real wallet note before fallback.
- Once real-note witnesses allow Cake Wallet to reach broadcast, preserve the exact PIVX Core rejection string. `bad-txns-shielded-requirements-not-met` still suggests anchor/nullifier or mempool requirements; spend/output/binding validation failures should move to the wallet/Core vector-comparison lane unless ElectrumX supplied inconsistent witness or anchor data.
- Return structured RPC errors for unsupported methods, daemon failures, invalid ranges, pruned data, and partial/index-incomplete responses. Do not return `null` for these cases.
- Add server tests for empty successful ranges, failed daemon ranges, response ordering, explicit output positions, nullifier status, anchor/witness consistency, and reorg rollback behavior.

## Prompt For ElectrumX Agent: RPC Contract

Use this prompt in the PIVX Sapling ElectrumX repository:

```text
Audit and implement the production PIVX Sapling ElectrumX v1 RPC contract for Cake Wallet. Start with capability discovery and `get_block_range` correctness. The server must distinguish empty successful ranges from failures, must return structured errors for daemon/index/method failures, and must never let a failed range look complete. Add or document method aliases for Cake Wallet's client needs: capability probe, block range, nullifier status, commitment info, best anchor, anchor height, tree state, and witness. Include tests for success-empty, daemon failure, unsupported method, invalid range, and partial/index-incomplete responses.
```

## Prompt For ElectrumX Agent: Global Positions And Witnesses

Use this prompt in the PIVX Sapling ElectrumX repository:

```text
Implement canonical global Sapling output position indexing for PIVX ElectrumX. `get_block_range` must return every Sapling output in PIVX Core canonical block transaction/output order with an explicit global position. Add witness responses that are anchor-bound and include anchor/root, anchor height, note position, path, and commitment. Add tests proving positions remain stable across restart, empty blocks do not consume positions, block response ordering is canonical, and witness paths verify against the requested anchor.
```

## Prompt For ElectrumX Agent: Witness Backend

Use this prompt in the PIVX Sapling ElectrumX repository if Cake Wallet still reports `canonical_witness_unavailable` or `witness_lookup_failed` for the anchor-bound attempt:

```text
Fix the anchor-bound PIVX Sapling witness backend for Cake Wallet shielded sends. The current deployed nodes advertise `pivx.sapling.electrumx.v1` and pass safe helper probes, including `get_best_anchor`, dummy nullifier status, and dummy commitment-info. A 2026-06-05 Cake Wallet iPhone 16e simulator z-to-z send now gets past real-note witness lookup via compatibility fallback: `get_witness(commitment, anchor)` is attempted first and fails with the wallet-sanitized reason `witness_lookup_failed`, then `get_witness(commitment)` returns a witness accepted by Cake Wallet, FFI proving completes, and broadcast returns a txid string. The sanitized broadcast shape was `bytes=2365 version=3 type=0 vin=0 vout=0 sapling=present value_balance=2365000 shielded_spends=1 shielded_outputs=2 binding_sig=present parse=ok`, with no `bad-txns-*` rejection in the captured log.

Implement/fix `blockchain.sapling.get_witness(commitment, anchor)` so it returns an anchor-bound witness for a real 32-byte Sapling note commitment/cmu. The response must include the requested commitment, global position, note height, requested anchor/root, anchor height, and exactly 32 path siblings. Each path sibling must be 64 hex chars and must be the canonical Sapling commitment-tree node encoding accepted by PIVX/librustpivx `zcash_primitives::sapling::Node::read`. Do not return block hashes, txids, note commitments, SHA branches, serialized DB blobs, reversed arbitrary bytes, or non-canonical field elements as path nodes. Add tests that reconstruct the path from commitment + position to the returned requested anchor and parse every sibling through the same canonical Sapling Node reader used by PIVX transaction proving. Keep `get_witness(commitment)` compatibility fallback working, but it is not sufficient for the v1 release contract because wallet spend construction should not have to switch anchors after selecting one.

Also verify post-broadcast `get_block_range` behavior at the chain tip. The same simulator run broadcast successfully but post-broadcast shielded sync failed and left the Cake Wallet dashboard showing `Shielded sync failed`; single-height ranges, including empty or just-mined tip heights, must return complete v1 envelopes with height/hash metadata or a structured retryable error, and must not look partially complete.

Acceptance: Cake Wallet simulator shielded send accepts `get_witness(commitment, anchor)` directly, without falling back to commitment-only; the fallback remains compatible; post-broadcast shielded sync completes without leaving `Shielded sync failed`; bundled default-node probe remains green for all four Chainster endpoints; and server tests cover real witness reconstruction to the requested anchor, canonical node parsing, commitment-only fallback anchor reporting, tip single-height block-range completeness, empty blocks, and reorg/rollback behavior. If Cake Wallet then reaches broadcast and receives a PIVX Core `bad-txns-*` rejection, preserve the exact rejection code and sanitized transaction-shape summary for the wallet/Core vector-comparison lane rather than masking it as a generic witness failure.

Cake Wallet now logs a compact `Witness source summary`; acceptance requires the simulator/device send to report `anchor_bound` and not `commitment_only_fallback`.
```

## Prompt For ElectrumX Agent: Default-Node Timeouts

Use this prompt in the PIVX Sapling ElectrumX repository or deployment runbook if Cake Wallet default-node probes show timeouts again:

```text
Investigate and fix PIVX Sapling ElectrumX default-node timeouts seen by Cake Wallet's wallet-secret-free probe. Current evidence from `fvm dart run tool/pivx_sapling_default_node_probe.dart` at 2026-06-05T11:08:52Z:

- `electrum01.chainster.org:50002` and `electrum01.chainster.org:50001` are healthy: both advertise `pivx.sapling.electrumx.v1`, pass `get_best_anchor`, dummy `get_nullifier_status`, structured dummy `get_commitment_info`, complete the sampled 100-block ranges, and return complete empty single-height current-tip/previous-tip `get_block_range` envelopes.
- `electrum02.chainster.org:50002` is reachable and advertises the v1 contract, but dummy `blockchain.sapling.get_commitment_info` times out, all sampled `blockchain.sapling.get_block_range` calls time out, and current-tip lookup is unavailable.
- `electrum02.chainster.org:50001` times out at `server.version`.

Find whether `electrum02` is still reindexing, has an unhealthy Sapling index, is blocked on PIVX Core RPC, has event-loop starvation, has SSL/plain listener issues, or is timing out because range/helper calls are doing unbounded synchronous work. Add instrumentation around request start/end, backend Core RPC calls, Sapling index lookups, range envelope assembly, and socket accept/read/write paths so each timeout can be attributed to index state, Core RPC latency, CPU/index scan time, or network/listener health.

Fix the deployment/code so every bundled endpoint either returns within the expected probe timeout or returns a structured retryable v1 error such as index_not_ready, backend_timeout, pruned_range, or partial_index. Do not let a node advertise `release_contract_ready=true` while helper/range calls can hang until the client timeout. If the Sapling index is rebuilding or behind tip, the capabilities response should expose that degraded state and `get_block_range`/helper calls should fail fast with structured errors rather than timing out.

Acceptance: rerun Cake Wallet's default-node probe and show all four Chainster endpoints reachable, `live_helper_methods_ready=true`, complete sampled activation-era ranges, complete empty/non-empty single-height current-tip ranges, and no raw timeouts. Add server/deployment tests or health checks covering reindex-in-progress, Core RPC unavailable/slow, empty tip block, just-mined tip block, large historical range, plain and SSL listeners, and structured retryable errors. Keep this separate from the anchor-bound `get_witness(commitment, anchor)` fix: both must pass before Cake Wallet can close default-node/send readiness.
```

## Prompt For ElectrumX Agent: Reorg And Policy

Use this prompt in the PIVX Sapling ElectrumX repository:

```text
Define and test PIVX Sapling reorg handling for the ElectrumX index used by Cake Wallet. Support rollback to at least 100 blocks unless product policy changes. Ensure returned block hashes let clients detect stale scanned state. Confirm and document mainnet/testnet Sapling activation heights from the canonical PIVX Core 5.6.1 source or chosen release commit. Add tests covering a reorg that removes Sapling outputs, a reorg that spends a nullifier on a different branch, and client rescan from the rollback boundary.
```
